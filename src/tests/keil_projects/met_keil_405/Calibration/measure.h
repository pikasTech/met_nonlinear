#ifndef __MEASURE_H
#define __MEASURE_H

#define moto_Left moto[0]   // ????
#define moto_Right moto[1]  // ????

#define SIGNAL_VALUE_BUF_NUM 3  // ?????????????????????

struct SIGNAL {                        // ??????
    float last[SIGNAL_VALUE_BUF_NUM];  // ??????????
    float now;                         // ?????
    float max;  // ???????????????????
};

struct ANGLE_UNIT {  // ????????
    float angle;     // ??????????
    float rad;       // ???
    float puls;  // ???????????????????
};

struct DISTENCE_UNIT {  // ????????
    float d;            // ??????????
    float angle;  // ????????????????????
};

// ??????
void updata_signal_buf(struct SIGNAL* p,
                       float new_value);  // ??????????
float cycle_value_adjust(
    float input,
    float cycle);  // ??????????????????????
float get_cycle_bios_with_adjust(float goal,
                                 float now,
                                 float cycle,
                                 float d_t);  // ???????????
float puls_to_angle_unit(float input,
                         int puls_num);  // ????????????

void measure_fun_1(void);  // ????1????????????

#endif
