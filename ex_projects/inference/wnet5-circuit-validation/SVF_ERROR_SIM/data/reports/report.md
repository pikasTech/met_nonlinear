# SVF层误差仿真结果报告

## 概述

本报告展示了SVF（状态可变滤波器）层误差仿真的结果，对比了理论计算与实际测量之间的差异。

- **项目**: WNET5q1h2u6l3
- **分析层数**: 1
- **频率范围**: 2 - 500 Hz
- **仿真模式**: 拟合传递函数
- **Dense层**: 包含

---

## 图片说明

### 1. SVF拟合对比图

**文件**: `plots/svf_fit_comparison.png`

**设计目的**: 验证拟合传递函数是否能够准确描述实测SVF层的频率响应特性。

**横轴**: 频率 (Hz)，范围 2-500 Hz，对数刻度

**纵轴**: 增益（线性），归一化幅度

**数据曲线**:
- **实线（Measured）**: 实际测量得到的SVF层频率响应数据
- **虚线（Fitted）**: 使用拟合传递函数参数计算得到的理论频率响应
- 每个子图对应一个SVF通道（SVF1_LP, SVF1_BP, SVF1_HP, SVF2_LP, SVF2_BP, SVF2_HP）

**数据来源**:
- 实测数据: `exam_data/20251230_SVFNET_SVF_ONLY/20251230_SVF_ONLY.xlsx`
- 拟合参数: 理论参数作为初始值，通过最小二乘法优化得到

---

### 2. SVF误差对比图

**文件**: `plots/svf_error_comparison_merged.png`

**设计目的**: 对比理想SVF层理论计算与实际测量之间的频率响应差异，分析误差分布。

**横轴**: 频率 (Hz)，范围 2-500 Hz，对数刻度

**纵轴**: 增益比值（理论/实测），单位 dB

**数据曲线**:
- 每条曲线代表一个SVF通道的误差比值
- 曲线值接近0 dB表示理论与实测一致
- 曲线偏离0 dB表示存在误差

**数据来源**:
- 理论值: 使用原始SVF参数（center_freqs, Q）通过传递函数计算
- 实测值: 从Excel测量文件读取

---

### 3. SVF+Dense误差对比图

**文件**: `plots/svf_dense_error_comparison.png`

**设计目的**: 展示包含Dense层时，SVF层误差对整体电路频率响应的影响。

**横轴**: 频率 (Hz)，范围 2-500 Hz，对数刻度

**纵轴**: 增益 (dB)

**数据曲线**:
- **实线（Ideal SVF + Dense）**: 使用理想SVF参数计算的完整电路频率响应
- **虚线（Measured SVF + Dense）**: 使用实测/拟合SVF参数计算的完整电路频率响应

**数据来源**:
- 理想SVF参数: 原始理论参数（center_freqs: [10.0, 80.0], Q: [1.0, 1.0]）
- 拟合SVF参数: 优化后的参数（center_freqs: [11.83, 84.11], Q: [0.9937, 0.9997]）
- Dense层权重: 从模型项目 `WNET5q1h2u6l3` 加载

---

## 结论

本仿真通过以下步骤完成SVF层误差分析：

1. **数据加载**: 从实测数据文件读取SVF层频率响应
2. **参数拟合**: 使用实测数据拟合传递函数参数（R10改进，幅度拟合 + 理论相位）
3. **误差计算**: 对比理论计算与实测数据的差异
4. **Dense层分析**: 将SVF误差传递到完整电路（SVF+Dense）

拟合质量评估（R² > 0.99 表明拟合良好）:

- 整体RMSE: 0.004530
- 整体R²: 0.999846


---

*报告生成时间: 自动生成*
