# R2.1.7 Interim Report

## 当前状态

- 目标：按共享骨架迁移旧 `qemu-c-inference` 路径的全部模型与全部功能，FRIKAN 仅作为首个验证锚点，而不是专项特例。
- 当前进度：`in_progress`

## 已完成

### 1. 新架构共享骨架

- 已建立 `src/core/board_inference/` 作为隔离重构包
- 已建立：
  - `entrypoints.py`
  - `comparison.py`
  - `registry.py`
  - `models/`
  - `platforms/benchmark_common.py`
  - `debug_cli.py`
- 当前 `debug_cli` 可直接比较：
  - 真实旧 `cli.py ep ...`
  - 新 `board_inference` 入口

### 2. horizontal preset 6 模型覆盖验证

已完成 `R2.1.7.2`：
- 静态识别 6 / 6 模型
- `qemu configured compare`：6 / 6 `success_match`
- `keil configured compare`：
  - `GRN / LSTMTransformer / LSTM / TCN` 为 `success_match`
  - `FRIKAN / 1DCNN` 为 `shared_failure`

相关报告：
- `C:\work\met_nonlinear_master\docs\MDTODO\details\07_推理BENCHMARK\R2.1.7.2_Task_Report.md`

### 3. 已补齐的共享副作用修复

为避免离线验证破坏旧仓库产物，已修复：
- `training_state.json.timestamp` 在非训练任务中的误刷新
- `load_weights()` 非训练重写 canonical `*.weights.json`
- `debug_cli compare` 对 Keil / serial / legacy_cli 副产物的误判

## 当前未完成

- 除 FRIKAN 外，其余模型当前仍主要通过 `legacy_fallback` 保证 wrapper 级一致性，尚未全部 native 化
- `legacy fallback` 仍存在，因此还不能宣称“新架构已经完全脱离 `src/core/lstm_qemu_ep_task.py`”

## 下一阶段

- 进入 `R2.2`：清理所有 `legacy fallback`
- 让 horizontal preset 内全部模型改为 native 执行
- 在 native 测试阶段临时将 `src/core/lstm_qemu_ep_task.py` 重命名为 `.bck`，验证新架构不会错误依赖旧模块
