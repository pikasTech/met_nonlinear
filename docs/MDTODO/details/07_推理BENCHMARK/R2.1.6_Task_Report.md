# R2.1.6 Interim Report

## 当前状态

- 目标：将 FRIKAN 核心逻辑独立迁移到 `src/core/board_inference/`，并完成 `qemu build/run`、`keil-bench`、性能数字、串口输出一致性验证。
- 当前进度：`in_progress`

## 已完成

- `FRIKAN` 已在新架构中拥有独立 native 实现：`C:\work\met_nonlinear_master\src\core\board_inference\models\frikan.py`
- `qemu configured compare` 已完成并达到 `success_match`
  - compare root: `C:\work\met_nonlinear_master\ex_projects\inference\qemu-c-inference\frikan_h8u6l6_e1k_lr7e4\debug_cli\20260421_171945_compare`
- 新架构 `debug_cli` 已默认对比真实旧 `cli.py ep ...` 主流程，而非平行 mock 流程
- `board_inference` compare 已补齐 host elapsed、JSONL 时间戳、Keil 副产物与 stream 噪声归一化

## 当前阻塞

- `keil configured compare` 中，`FRIKAN` 旧/新均失败，分类为 `shared_failure`
  - compare root: `C:\work\met_nonlinear_master\ex_projects\inference\qemu-c-inference\frikan_h8u6l6_e1k_lr7e4\debug_cli\20260421_172413_compare`
  - 共享失败原因：Keil 串口验证输出解析时，旧/新都报 `could not convert string to float`
- 该问题已按用户约束冻结为“共享故障只记录、不修旧路径”

## 结论

- FRIKAN native 的 `qemu` 一致性已经完成
- FRIKAN native 的 `keil-bench` 尚未达到“成功完成并一致”的终态，因此 `R2.1.6` 继续保持 `in_progress`
