# R1.1 任务报告：主文压缩与 Supplement 分流执行

## 1. 任务范围

本任务执行 `docs/MDTODO/20260531-MN最后冻结投稿.md` 中的 R1.1：按照 R1 计划，将 MN 投稿主文压缩到 6000 英文词以内，并把原计划迁移的推导、协议、部署和扩展实验细节转入 Supplement。

执行边界如下：

- 只做主文与 Supplement 之间的格式分流和轻量压缩。
- 沿用原有术语体系，不引入新概念。
- 不修改实验指标定义、数值结果、canonical 项目或科学结论。
- 迁移时保留完整 `\CNEN{中文}{英文}` 单元，避免只迁移英文槽或丢弃中文中间稿。
- 主文保留核心故事线和主要证据，Supplement 承接细节、推导、扩展图表和部署说明。

## 2. 修改文件

本轮直接修改和生成的文件如下：

- `docs/paper/latex/main.translated.tex`
- `docs/paper/latex/supplement.tex`
- `docs/paper/latex/backup/main.translated_20260531_R1_1_pre_migration.tex`
- `docs/MDTODO/details/20260531-MN最后冻结投稿/20260531_155741_Task_Report.md`

本轮临时工作文件已清理：

- `.tmp_mn_r1_1_migrate.py`
- `.tmp_dataset.txt`
- `.tmp_method.txt`
- `.tmp_results_disc_conclusion.txt`

## 3. 主文压缩结果

主文现在保留以下章节骨架：

- Introduction
- Background and Principles
- Experimental Design and Dataset
- Proposed Method
- Experiments and Results
- Discussion
- Data Availability
- Conclusion

主文保留的 display items 为 6 图 + 2 表，共 8 个：

| 类型 | Label | 内容定位 |
| --- | --- | --- |
| Figure | `fig:met_structure_readout` | MET 结构与读出 |
| Figure | `fig:experimental_setup_dataset_workflow` | 实验平台与数据流程 |
| Figure | `fig:MET_nonlinear` | 非线性响应主证据 |
| Figure | `fig:wiener_kan_framework` | Wiener-KAN 框架 |
| Figure | `fig:frequency_response_comparison` | 主模型频响补偿结果 |
| Table | `tab:models` | 横向模型配置 |
| Figure | `fig:horizontal_summary` | 横向比较摘要 |
| Table | `tab:ablation_overview` | 消融实验总览 |

`latex-cli.py word-count` 对 `main.translated.tex` 的结果为：

| 部分 | English words | CJK chars | Numbers | Total units |
| --- | ---: | ---: | ---: | ---: |
| Title | 10 | 0 | 0 | 10 |
| Abstract | 14 | 463 | 1 | 478 |
| Body | 3703 | 5610 | 33 | 9346 |

说明：该计数器会同时统计 `\CNEN` 中文槽和英文槽。面向 MN 投稿长度判断时，当前相关英文正文为 3703 words，低于 6000 words 目标；`total_units` 偏高来自保留中文中间稿。

`latex-cli.py figure-table-count` 对主文的结果为：

| Figures | Tables | Total floats |
| ---: | ---: | ---: |
| 6 | 2 | 8 |

## 4. Supplement 分流结果

新增 `docs/paper/latex/supplement.tex`，复用主稿的 SN 模板、数值宏和 `\CNEN` 双语机制，并默认使用 `\UseCNENEnglish` 渲染英文 Supplement。

Supplement 承接了以下模块：

| Supplement note | 迁移内容 |
| --- | --- |
| S1 | MET 机理、小信号模型、多物理非线性公式与机理图 |
| S2 | 并联 Wiener 等效建模、三支路频响复现图 |
| S3 | 实验设置表、数据集协议表、数据张量和预处理细节 |
| S4 | KAN 映射、单神经元补偿示例、局部传递函数切片和 IIR 初始化 |
| S5 | AFMAE 的连续/离散能量推导和总损失定义 |
| S6 | LUT 查询、插值、嵌入式验证流程和板端实现变体 |
| S7 | 扩展消融、约束分解、Wiener 前端、超参数敏感性与 loss 收敛图 |

`latex-cli.py figure-table-count` 对 Supplement 的结果为：

| Figures | Tables | Total floats |
| ---: | ---: | ---: |
| 7 | 3 | 10 |

Supplement 中对主文图表的引用已改为固定主文编号文本，避免独立编译时出现 unresolved reference。S7 图的版式经过微调，最终避免了图排到 References 后的问题，也消除了自动 layout-check 的 underfilled page 告警。

## 5. 构建与校验

### 主文构建

命令链路：

```powershell
python C:\Users\lyon\.agents\skills\latex\scripts\latex-cli.py build --workdir C:\work\met_nonlinear_master\docs\paper\latex --tex main.translated.tex
```

最终 job：`20260531_172549_74aa9979`

| 项 | 结果 |
| --- | --- |
| status | done |
| output PDF | `docs/paper/latex/build/main.translated.pdf` |
| layout_check | passed |
| body English words | 3703 |
| total floats | 8 |

### Supplement 构建

命令链路：

```powershell
python C:\Users\lyon\.agents\skills\latex\scripts\latex-cli.py build --workdir C:\work\met_nonlinear_master\docs\paper\latex --tex supplement.tex
```

最终 job：`20260531_175954_cec64a5f`

| 项 | 结果 |
| --- | --- |
| status | done |
| output PDF | `docs/paper/latex/build/supplement.pdf` |
| layout_check | passed |
| body English words | 5553 |
| total floats | 10 |
| PDF pages | 19 |

### paper-spec 扫描

`paper-spec-cli.py scan` 对主文和 Supplement 均无 error，仅有 warning：

| 文件 | warning 数 | 说明 |
| --- | ---: | --- |
| `main.translated.tex` | 17 | 主要为 `约束`、`需求`、`围绕` 等规则命中；上下文多为物理约束、数学约束、实时处理需求或消融分组描述。 |
| `supplement.tex` | 29 | 主要为 `约束` 规则命中；上下文多为 KAN 结构约束、奇对称性、正值性、实时性约束和损失约束。 |

本轮 R1.1 的目标是格式分流与压缩，不改变实质内容；这些 warning 未阻塞构建，也未发现正文泄露修改过程、任务路径或编辑痕迹。

### 残留痕迹扫描

对 `main.translated.tex` 与 `supplement.tex` 做了定向字符串扫描，检查项包括：

- `TBD` / `TODO` / `FIXME`
- `??`
- `docs/paper`、`docs/MDTODO`、`MDTODO`、`.tmp_`、`Task_Report`
- `为回应`、`当前版本`、`当前修订`、`修订版`、`point-to-point`、`旧稿`、`修改说明`、`任务报告`
- 常见裸损坏片段 `oindent`、`extbf`、`abla` 等

结果：

| 文件 | 结果 |
| --- | --- |
| `main.translated.tex` | 0 命中 |
| `supplement.tex` | 无 TODO、内部路径、修订痕迹或裸损坏；扫描到的 `eq f_j` 均来自合法数学式 `\neq f_j`，判定为误报。 |

## 6. 风险与说明

- 主文英文正文已满足 6000 词以内目标；由于源文件保留中文槽，机器总单位数不等同于投稿英文词数。
- 主文 display items 已控制为 8 个，符合 R1 计划中的目标上限。
- Supplement 已独立编译通过，并保留迁移内容的完整 `\CNEN` 结构。
- 本轮未更新实验结果、指标数值、模型配置或数据来源，仅调整主文与 Supplement 的组织方式。
- 仍建议后续在最终投稿前做一次人工通读，重点检查 Supplement 中固定主文编号文本是否与最终主文浮动体排序保持一致。

## 7. 结论

R1.1 已完成：主文已压缩为英文正文约 3703 words，display items 控制为 8 个；详细推导、协议、部署和扩展消融材料已转入 Supplement；主文与 Supplement 均已通过 LaTeX 构建和 layout-check。科学内容、指标和结论未做实质性变更。