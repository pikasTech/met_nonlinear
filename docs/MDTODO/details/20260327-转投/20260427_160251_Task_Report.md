# R24.3 Task Report

## 任务目标

将 Table 5 (`tab:ablation_overview`) 中重复出现在多个 group 的 canonical baseline 折叠为首行单独展示，并通过 paper pipeline 自动生成更新后的表格，避免手工修改 LaTeX 片段。

## 修改内容

- 修改 `docs/paper/src/paper_pipeline.py` 中的 `build_ablation_overview_rows()`：
  - 将共享的 Wiener-KAN baseline 折叠为单独首行；
  - baseline `Group` 设为 `Baseline`，`Variant` 设为 `B-spline + Odd+pos. + System, fixed, + MAE + AFMAE`；
  - 为避免静默吞掉不一致数据，新增 baseline 一致性校验：若 `Mapping/Wiener front/Loss` 三处 baseline 指标不一致则直接报错。
- 修改 `write_ablation_overview_tex()`：对带 `emphasize` 标记的行整行输出 `\textbf{...}`，因此 baseline 的组名、variant、contrast 与三项指标会一起加粗。
- 修改 `build_tables()` 的 Markdown 总表生成：`docs/paper/data/generated_tables.md` 中的 baseline 首行同步加粗，保持 paper pipeline 导出的两份总表口径一致。
- 重新执行 `conda run --no-capture-output -n tf26 python docs/paper/gen_data.py`，刷新：
  - `docs/paper/data/results.json`
  - `docs/paper/data/generated_tables.md`
  - `docs/paper/latex/values.tex`
  - `docs/paper/latex/tables/ablation_overview.tex`

## 结果检查

- `docs/paper/latex/tables/ablation_overview.tex` 现在以加粗的 baseline 首行开头，后续 `Mapping/Constr./Wiener front/Loss` 四组只保留非 baseline 对比项。
- `docs/paper/data/results.json` 中 `ablation_overview` 行数更新为 `10`，且首行 group 为 `Baseline`。
- `docs/paper/latex/values.tex` 中 `\valAblationOverviewRowCount` 更新为 `10`。
- 通过 LaTeX skill 构建 `docs/paper/latex/main.tex`，job `20260427_161316_79114eaf` 返回 `status=done`、`returncode=0`，`layout_check.status=passed`、`issue_count=0`。

## 验证命令

- `conda run --no-capture-output -n tf26 python docs/paper/gen_data.py`：成功。
- `python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py debug probe --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex`：通过。
- `python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex`：成功，job `20260427_161316_79114eaf`。
- `python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py status --job-id 20260427_161316_79114eaf`：完成，`layout_check.status = passed`。
