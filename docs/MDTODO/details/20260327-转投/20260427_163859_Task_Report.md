# R24.8 任务报告：修复 Fig. 12 工作流子图纵向压扁问题

## 任务目标

修复 `Fig. 12 Embedded inference validation workflow and performance.` 中前两行工作流子图的纵向压扁问题：

- (a) `trained Wiener-KAN`、`C export package`
- (b) `QEMU run`、`STM32F405`、`QEMU-MAE`、`KEIL metrics`

并通过现有 paper pipeline 同步到论文正文引用的最终图片产物。

## 修改内容

- 修改 `docs/paper/src/paper_pipeline.py`
  - 在 `make_board_inference_validation_workflow_figure()` 中重新调整了 (a)/(b) 两个 workflow panel 的画布高度、卡片尺寸、图标尺寸和箭头锚点。
  - 重点增加了被点名节点的纵向空间，避免图标和卡片在横向布局下显得过扁。
  - 保持原有语义、颜色和流程结构不变，只修正布局比例。
- 修改 `docs/paper/src/paper_pipeline.py`
  - 在 `make_onboard_figure()` 中改为每次先重生共享的 board-inference workflow panel，再拼接 Fig. 12。
  - 这样 `fig_17_board_inference_validation_workflow.png` 和 `fig_05_onboard_inference.png` 会始终使用同一版最新 workflow 布局，避免只更新 standalone workflow 而 Fig. 12 继续引用旧 panel。

## 产物同步

运行 paper pipeline 后，以下正文引用产物已更新：

- `docs/paper/figures/fig_17_board_inference_validation_workflow.png`
- `docs/paper/figures/fig_05_onboard_inference.png`
- 对应 `.raw.json` 与 `_montage_panels/` 中的中间 panel 产物
- `docs/paper/data/results.json`
- `docs/paper/latex/values.tex`

由于 `docs/paper/latex/main.tex` 直接引用 `../figures/fig_05_onboard_inference.png`，因此正文中的 Fig. 12 已自动吃到新图，无需手工改正文。

## 结果检查

- (a) 中 `trained Wiener-KAN` 与 `C export package` 图案的纵横比例已恢复到更自然的状态。
- (b) 中 `QEMU run`、`STM32F405`、`QEMU-MAE` 与 `KEIL metrics` 的图案和卡片纵向空间已明显增加，不再呈现被压扁的观感。
- Fig. 12 顶部 workflow row 已随共享 panel 一并刷新，不会再与 standalone workflow 图出现不同步。

## 验证

- `C:\Users\lyon\MiniConda3\envs\tf26\python.exe docs/paper/gen_figures.py`
  - 成功，重新生成论文 figures、`results.json` 和 `values.tex`
- 本地目视检查：
  - `docs/paper/figures/fig_17_board_inference_validation_workflow.png`
  - `docs/paper/figures/fig_05_onboard_inference.png`
  - 确认被点名子图的纵向压扁问题已修复
- `C:\Users\lyon\MiniConda3\envs\tf26\python.exe C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir C:/work/met_nonlinear_master/docs/paper/latex --tex main.tex --engine xelatex --passes 2`
  - 启动成功，`paper_spec_scan.result.passed = true`
  - 第 1 次 XeLaTeX pass 已产出更新后的 `docs/paper/latex/build/main.pdf`
  - 第 2 次 pass 失败，报错定位在 `docs/paper/latex/build/main.aux:71`，对应 `fig:parallel_wiener_modeling` 的辅助文件读取问题；该问题出现在并联 Wiener 图的 caption/aux 链路，不是本次 Fig. 12 布局修改引入的问题

## 结论

R24.8 要求的 Fig. 12 workflow 子图压扁问题已经修复，并且已经通过 paper pipeline 同步到正文实际引用的图像产物。当前唯一残留的是一次独立的多 pass LaTeX 辅助文件问题，定位不在本任务修改范围内。