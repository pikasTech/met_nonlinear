# R22.1 任务报告

## 任务目标

按 `docs/MDTODO/20260327-转投.md` 的 R22.1 要求，统一处理论文多子图拼接方式：将指定图改为使用 `src/visualization/subfigure_montage.py` 的统一 bitmap montage 模块组合，并去掉子图标签 `(a)`、`(b)` 外侧框线。

## 已完成修改

1. 统一拼图入口
   - 更新 `docs/paper/src/paper_pipeline.py`，为论文流水线增加 `_montage_panels` 面板输出与 `make_bitmap_montage()` 的无框标签默认行为。
   - `make_bitmap_montage()` 现在默认 `label_box=False`，后续通过该入口生成的 `(a)`、`(b)` 等标签不再绘制边框。
   - `save_figure_raw_files()` 改为合并现有 raw 记录，避免后续统一数据写入覆盖 montage 溯源信息。

2. 指定图的 montage 改造
   - `Experimental setup and dataset construction workflow.` 改为左右拼接。
   - `Parallel Wiener equivalent modeling of the amplitude-dependent MET response.` 改为第一行 `(a)` 原理图、第二行 `(b)` center-frequency 曲线与 `(c)` 100 Hz gain 曲线；第二行两张曲线图由统一拼图模块组合。
   - `Local linear frequency responses identified at fixed input magnitudes.` 改为在 `docs/paper/src/translate_legacy_figures.py` 中调用统一拼图模块组合。
   - `Principle of the AFMAE training objective.`、`LUT inference principles for embedded Wiener-KAN deployment.`、`Embedded inference validation workflow.` 均改为先生成独立面板，再用统一拼图模块组合。
   - `Effectiveness of the unified Wiener-KAN main model in reducing amplitude-dependent frequency-response drift.` 改为 2 x 2 统一拼图。
   - `(a) Embedded inference throughput comparison. (b) LUT accuracy versus memory trade-off.` 改为两面板统一拼图。
   - `Main benchmark summary.` 改为 2 x 2 统一拼图。
   - `Metric ranges across representative models.` 改为三面板统一拼图。
   - `One-factor hyperparameter sensitivity around the canonical Wiener-KAN configuration.` 改为 2 x 3 统一拼图，legend 保持在第 2 行第 3 列。
   - `MAE, AFMAE, and joint-loss convergence curves.` 改为三条收敛曲线独立面板后统一拼图，并将 LaTeX 引用宽度调整为 `0.98\textwidth` 以保证可读性。

3. LaTeX 文稿调整
   - 更新 `docs/paper/latex/main.tex` 中 Parallel Wiener 图的 caption，将第二行曲线拆分为 `(b)` center-frequency trajectory 与 `(c)` 100 Hz gain trajectory。
   - 更新 `fig_03_loss_ablation.png` 的 `\includegraphics` 宽度以匹配三面板横向 montage。

## 重新生成的主要产物

- `docs/paper/figures/fig_21_experimental_setup_dataset_workflow_montage.png`
- `docs/paper/figures/fig_22_parallel_wiener_equivalent_montage.png`
- `docs/paper/figures/local_transfer_slices.png`
- `docs/paper/figures/fig_18_afmae_loss_principle.png`
- `docs/paper/figures/fig_15_lut_lookup_principles.png`
- `docs/paper/figures/fig_08_frequency_response_comparison.png`
- `docs/paper/figures/fig_17_board_inference_validation_workflow.png`
- `docs/paper/figures/fig_05_onboard_inference.png`
- `docs/paper/figures/fig_02_horizontal_summary.png`
- `docs/paper/figures/fig_13_compensation_distribution.png`
- `docs/paper/figures/fig_18_hparam_sensitivity.png`
- `docs/paper/figures/fig_03_loss_ablation.png`
- 对应 `*.raw.json` 已保留 montage 元数据。

## 验证结果

- 执行 `python docs/paper/gen_data.py` 成功，刷新论文数据、图像和 `values.tex`。
- 执行 `python -c "from docs.paper.src.translate_legacy_figures import plot_frirnn; plot_frirnn()"` 成功，刷新 `local_transfer_slices.png`。
- 执行 `python -m py_compile docs/paper/src/paper_pipeline.py docs/paper/src/translate_legacy_figures.py` 成功。
- 抽查 12 个目标图的 raw 记录，均包含 `montage` 元数据；其中 `fig_08_frequency_response_comparison` 为 2 x 2，`fig_18_hparam_sensitivity` 为 2 x 3。
- 执行 LaTeX 3-pass 构建：`python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir docs/paper/latex --tex main.tex --engine xelatex --passes 3`，job `20260426_111617_d12d0741` 完成，`returncode=0`。
- LaTeX layout check 通过：`issue_count=0`，输出 PDF 为 `docs/paper/latex/build/main.pdf`。
- 单独执行 paper-spec scan：job `20260426_112151_e16d622a` 通过，`finding_count=0`。

## 备注

首次 2-pass LaTeX 构建在第二遍遇到生成的 `main.aux` 临时异常；已停止残留 `xelatex` 进程、清理本次失败构建产生的 `main.*` 临时文件，并重新执行 3-pass 构建通过。该处理仅涉及 LaTeX build 目录下的本次生成产物。
