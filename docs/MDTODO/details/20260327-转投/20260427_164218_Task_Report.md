# R24.9 任务报告：调整 Fig. 12 为左列工作流、右列性能图布局

## 任务目标

将 `Fig. 12 Embedded inference validation workflow and performance` 的拼版从“上方 workflow row + 下方 (c) 性能图”改为：

- 左列：`(a)` 与 `(b)` 上下排列的 workflow
- 右列：`(c)` 性能散点图跨两行占满第二列

并通过现有 paper pipeline 同步到论文正文实际引用的图像与 PDF。

## 修改内容

- 修改 `docs/paper/src/paper_pipeline.py`
  - 在 `make_onboard_figure()` 中不再临时生成 `fig_05_onboard_inference_ab_workflow.png` 作为顶行拼图。
  - 改为直接复用共享的 `fig_17_board_inference_validation_workflow.png` 作为左列整列 workflow，确保 `(a)` / `(b)` 保持上下堆叠。
  - 将 Fig. 12 的最终 montage 从 `vertical` 改为 `horizontal`，形成“左列 workflow + 右列 (c)”的两列排布。
  - 为了让 `(c)` 在单栏宽度下与左列高度更接近，将性能散点图的 `figsize` 从横向比例改为更高的纵向比例，并按 `fit_height` 对左右两列统一缩放。
  - 更新 montage 的 padding / gutter / note，使最终成图更均衡。
- 未修改 `docs/paper/latex/main.tex`
  - 正文仍直接引用 `../figures/fig_05_onboard_inference.png`，因此只要 paper pipeline 刷新图像产物，Fig. 12 会自动同步，无需手工改正文。

## 产物同步

重新执行 paper pipeline 后，以下产物已刷新：

- `docs/paper/figures/fig_05_onboard_inference.png`
- `docs/paper/figures/fig_05_onboard_inference.raw.json`
- `docs/paper/figures/fig_17_board_inference_validation_workflow.png`
- `docs/paper/figures/_montage_panels/fig_05_onboard_inference_c_embedded_performance.png`
- `docs/paper/data/results.json`
- `docs/paper/latex/values.tex`

## 结果检查

- Fig. 12 现在变为左列 `(a)` / `(b)` workflow、右列 `(c)` 性能图的两列布局。
- `(a)` 与 `(b)` 不再被挤在同一横排里，阅读顺序更清晰。
- `(c)` 的高度与左列整体更接近，整幅图在单栏正文里的视觉重心更均衡。
- 由于沿用了 `fig_17_board_inference_validation_workflow.png`，R24.8 修复过的 workflow 图标纵横比例也被完整继承到 Fig. 12。

## 验证

- `C:\Users\lyon\MiniConda3\envs\tf26\python.exe docs/paper/gen_figures.py`
  - 成功，paper pipeline 已重生图像、`results.json` 和 `values.tex`。
- `C:\Users\lyon\MiniConda3\envs\tf26\python.exe C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py debug probe --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex`
  - 成功，确认 LaTeX 工作目录与引擎解析正常。
- `C:\Users\lyon\MiniConda3\envs\tf26\python.exe C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex`
  - `paper_spec_scan.result.passed = true`。
  - 该 wrapper 在本次构建里仍出现 `xdvipdfmx` 结束异常；随后改用前台 `xelatex -no-pdf` + `xdvipdfmx` 直接生成有效 PDF。
- `D:\texlive\2024\bin\windows\xelatex.EXE -no-pdf -interaction=nonstopmode -file-line-error -output-directory=build main.tex`
  - 成功，产出 `docs/paper/latex/build/main.xdv`。
- `D:\texlive\2024\bin\windows\xdvipdfmx.EXE -o build/main.pdf build/main.xdv`
  - 成功，产出有效的 `docs/paper/latex/build/main.pdf`。
- `D:\texlive\2024\bin\windows\pdftoppm.EXE -png -f 21 -singlefile build/main.pdf build/r24_9_page21`
  - 成功，抽查 PDF 第 21 页可见新的 Fig. 12 已按“两列布局”落入正文。

## 结论

R24.9 要求的 Fig. 12 重排已经完成，并且已经通过 paper pipeline 同步到论文实际引用的图像产物与最新 PDF。当前正文中的 Fig. 12 已是“左列 workflow、右列性能图”的新布局。
