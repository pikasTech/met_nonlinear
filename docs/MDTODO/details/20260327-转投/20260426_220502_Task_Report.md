# R23.2.1 Task Report

## 任务目标

按用户要求整改 Table 5 `Numerical overview of Wiener-KAN ablation studies.`：移除总表中的 `Detail` 展示列、避免 `Hparams` 出现在总表分组中、合并相同 `Group` 单元格、不在组间增加虚线区隔，并删除后续与总表重复的消融子表。

## 修改内容

1. 更新 `docs/paper/src/paper_pipeline.py`：
   - `ablation_overview` 的 LaTeX 输出改为 6 列：`Group`、`Variant / axis`、`Controlled contrast`、三项物理指标。
   - 移除 `Detail` 列的 TeX、Markdown 和 JSON 行级输出，不再保存已删除子表的 detail label。
   - 使用 `\multirow` 合并连续相同的 `Group` 单元格；只保留表头和表尾横线，不在组间插入虚线或额外分隔线。
   - 将原 `Hparams` 分组显示改为 `Sensitivity`，保留超参数敏感性数据但避免表内出现 `Hparams` 分组文本。
   - 为 Table 5 输出增加适度 `\arraystretch`，改善删除子表后的版面密度。

2. 更新 `docs/paper/latex/main.tex`：
   - Results 消融段的总表引导语改为直接说明 Table 5 覆盖非线性映射、物理约束、Wiener 前端、超参数敏感性和损失函数。
   - 删除与 Table 5 重复的四个子表：
     - `tab:constraint_matrix`
     - `tab:wiener_frontend_ablation`
     - `tab:hyperparameter_sensitivity`
     - `tab:loss_ablation`
   - 将正文中对这些子表的引用统一改为引用 `tab:ablation_overview` 的对应分组或保留相关图引用。
   - 修复 paper-spec 扫描命中的 `后续` 预告式表述，将其改为 `扩展验证`。

3. 重新生成论文数据与表格：
   - 更新 `docs/paper/latex/tables/ablation_overview.tex`。
   - 更新 `docs/paper/data/generated_tables.md` 和 `docs/paper/data/results.json`，使 Table 5 的 Markdown/JSON/LaTeX 来源保持一致。
   - `docs/paper/latex/values.tex` 由 pipeline 同步刷新。

## 验证结果

- `python docs/paper/gen_data.py`：执行成功，刷新数据、表格和 values；运行时仅出现本机 RDRND warning。
- `python -m compileall docs/paper/src/paper_pipeline.py`：通过。
- `paper-spec scan --path docs/paper/latex/main.tex`：通过，0 findings；最新独立 job `20260426_223239_c33aeefa` 通过。
- LaTeX 编译：`bibtex build/main` 后连续执行 `xelatex -interaction=nonstopmode -file-line-error -output-directory=build main.tex`，最终 returncode 为 0，输出 `docs/paper/latex/build/main.pdf`，最终日志中无 undefined reference/citation warning；仍有一次 label rerun warning 和 Table 5 内 1.45982 pt 的轻微 overfull hbox。
- 自动 layout-check：仍为 failed，但仅剩 Table 5 所在 page 26 的 `underfilled_page` 与 Table 5 内 1.45982 pt 的轻微 `overfull_hbox`；Table 5 版面已通过截图人工检查，列结构、合并单元格和重复子表删除均符合本任务要求。

## 完成状态

Table 5 已按要求移除 `Detail` 列和 `Hparams` 分组文本，合并相同 `Group` 单元格，未添加组间虚线区隔；后续重复消融子表已从正文删除，正文引用已同步改写。R23.2.1 已标记完成。

