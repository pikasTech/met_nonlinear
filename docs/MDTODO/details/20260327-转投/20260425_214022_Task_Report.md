# R20.6 任务报告

## 任务目标

审查 `docs/paper/latex/main.tex` 全文，清理“块对角矩阵加速 Wiener 计算”相关残余。该机制只属于 PC 推理实现层面的向量化/矩阵化路径，不应继续作为论文正文或嵌入式部署论证的一部分出现。

## 审查范围

- 主审查文件：`docs/paper/latex/main.tex`
- 重点关键词：`块对角`、`block diagonal`、`block matrix`、`矩阵加速`、`PC 推理`、`Wiener 加速`、`并行线性层`、`并联处理`
- 重点段落：Wiener 线性层初始化、IIR 到 LRNN 映射、多核 Wiener 线性层、嵌入式部署与 LUT 部署段落

## 审查结论

`main.tex` 中未发现显式“块对角矩阵”“block diagonal”“PC 推理”“Wiener 加速”等残留表述。正文中仍存在两处容易被误读为 PC 端矩阵化并行加速的写法，集中在 Wiener 线性层段落：

1. “并行线性层”容易让读者误解为计算加速实现，而该段实际想表达的是多个震级工作点局部线性核共同构成多输出 Wiener 层。
2. “并联处理同一输入”与 `FRIRNN_parallel` 标签容易和此前块对角矩阵并行实现残余关联。

## 已完成修改

- `docs/paper/latex/main.tex:484`：将“构建包含多个工作点线性核的并行线性层”改为“将多个工作点线性核组织为同一层的多输出响应”，保留模型结构含义，删除加速实现暗示。
- `docs/paper/latex/main.tex:593`：将“并联处理同一输入”“多核并行线性层”改为“局部线性核分别作用于同一输入”“多核 Wiener 线性层”，明确这是结构描述而非 PC 端并行加速。
- `docs/paper/latex/main.tex:602`：将公式标签从 `eq:FRIRNN_parallel` 改为 `eq:FRIRNN_multikernel`，同步更新正文引用，避免源文件层面的 parallel 残余。

## 保留内容说明

以下内容经审查后保留，因为它们与“块对角矩阵加速 Wiener 计算”无关：

- 三支路并联 Wiener 等效模型：这是用于解释幅值相关响应迁移的物理/系统辨识模型，不属于 PC 推理加速。
- 数据集频率--震级矩阵、AFMAE response matrix、constraint matrix：这些是实验设计、损失函数或约束消融的正常术语。
- KAN LUT 加速：这是面向嵌入式部署的查表/插值路径，且正文已有 KEIL/QEMU 验证支撑。

## 验证结果

- 关键词复查：`rg` 未再检出 `块对角`、`block diagonal`、`block matrix`、`矩阵加速`、`PC 推理`、`Wiener 加速`、`并行线性层`、`并联处理`、旧标签 `FRIRNN_parallel`。
- `paper-spec` 扫描：`20260425_221011_4e996100`，`passed=true`，`finding_count=0`。
- `paper-spec` CLI 自检：`pyright` 通过，`0 errors, 0 warnings`。
- LaTeX 编译验证：`20260425_221053_a61a9523`，`xelatex` 两轮构建 `returncode=0`，输出 `docs/paper/latex/build/main.pdf`，`layout_check.status=passed`，未发现布局问题。

## 后续建议

R20.6 已完成。若后续继续压缩方法部分篇幅，可进一步把 IIR 到 LRNN 的矩阵推导收敛为较短的初始化公式说明，但这属于篇幅优化，不属于本任务的块对角矩阵残余清理范围。
