# R24.6 任务报告：移除过时的相对加速比表述

## 任务目标

将正文中“相对 LSTM 可实现 2.1× 速度提升”的过时表述替换为绝对板端推理数值，并确保该数值继续通过 paper pipeline 自动回填，而不是在 `main.tex` 中手写固定数字。

## 修改内容

- 修改 `docs/paper/latex/main.tex`
  - 将引言中的句子从相对加速比表述：
    - `相对 LSTM 可实现 \valSpeedup~$\times$ 的速度提升`
  - 改为绝对吞吐率表述：
    - `当前主实现的 KEIL 板端吞吐率达到 \valPrimaryKeilFps{} points/s`
- 修改 `docs/paper/src/paper_pipeline.py`
  - 在 `write_values_tex()` 的 `stale_macro_patterns` 中加入 `valSpeedup`；
  - 这样每次重新跑 paper pipeline 时，旧的硬编码宏会被自动清理，避免过时的相对加速比再次残留在 `values.tex` 中。

## 自动回填验证

- 重新执行 paper pipeline，刷新：
  - `docs/paper/data/results.json`
  - `docs/paper/data/generated_tables.md`
  - `docs/paper/latex/values.tex`
- 刷新后确认：
  - `docs/paper/latex/main.tex` 已不再引用 `\valSpeedup`
  - `docs/paper/latex/values.tex` 中旧的 `\valSpeedup` 宏已被清理
  - 引言中的绝对数值继续通过 `\valPrimaryKeilFps` 自动填充

## 结果检查

- 当前引言表述已改为：
  - `当前主实现的 KEIL 板端吞吐率达到 \valPrimaryKeilFps{} points/s`
- 该宏当前由 paper pipeline 根据主横评 `Wiener-KAN` 行自动写入 `values.tex`，因此后续如果板端吞吐率更新，正文会随 pipeline 重算而同步更新。

## 验证命令

- `C:\Users\lyon\MiniConda3\envs\tf26\python.exe docs/paper/src/paper_pipeline.py`
  - 成功，重新生成 figures / `data/results.json` / `data/generated_tables.md` / `latex/values.tex`
- `C:\Users\lyon\MiniConda3\envs\tf26\python.exe C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir C:/work/met_nonlinear_master/docs/paper/latex --tex main.tex --engine xelatex --passes 2`
  - 成功，job `20260427_162841_f0166fa2`
- `C:\Users\lyon\MiniConda3\envs\tf26\python.exe C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py status --job-id 20260427_162841_f0166fa2`
  - `status = done`
  - `returncode = 0`
  - `paper_spec_scan.result.passed = true`
  - `layout_check.status = passed`
  - `layout_check.issue_count = 0`
