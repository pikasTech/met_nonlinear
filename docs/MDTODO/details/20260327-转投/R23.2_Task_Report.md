# R23.2 任务报告：消融总表数值化与数据链统一

## 执行范围

- 修改 `docs/paper/src/paper_pipeline.py`，新增消融总览行构造、Markdown/TeX 双端输出和相关 `values.tex` 宏生成。
- 修改 `docs/paper/latex/main.tex`，将原定性 `tab:ablation_overview` 改为读取自动生成的数值总表，并补充总表引导语。
- 新增 `docs/paper/latex/tables/ablation_overview.tex`，作为正文消融总表的生成式 TeX 片段。
- 重新生成 `docs/paper/data/results.json`、`docs/paper/data/generated_tables.md`、`docs/paper/data/values_raw.json` 和 `docs/paper/latex/values.tex`。

## 完成内容

### 1. 消融总表升级为数值总表

`tab:ablation_overview` 已由定性分组说明改为 19 行数值总览，覆盖：

- KAN/MLP 与结构映射：B-spline、CNN-KAN、FRIMLP；
- 物理约束：odd+positive、positive-only、odd-only；
- Wiener 前端：系统先验/随机初始化与 fixed/trainable 四组；
- 超参数敏感性：canonical 基线及 h/u/l/g/s 五个敏感性轴；
- Loss 消融：MAE+AFMAE、MAE、AFMAE。

表格统一展示 `Freq Drift (Hz)`、`Sens Drift (%)`、`Linearity (%)` 和 `Compute Cost`，最后一列指向细分表格，避免 Results 中的消融证据分散。

### 2. 数据来源统一

- `docs/paper/src/paper_pipeline.py` 新增 `build_ablation_overview_rows()`，总表行直接从已有 `structure_ablation`、`wiener_frontend_ablation`、`loss_ablation` 和 `hparam_sensitivity` 数据结构组合生成。
- `docs/paper/data/results.json` 新增 `ablation_overview` 数组，作为总表的结构化数据快照。
- `docs/paper/data/generated_tables.md` 新增 `Ablation overview` 机器可读总表。
- `docs/paper/latex/tables/ablation_overview.tex` 由同一组 `ablation_overview` 行生成。
- `docs/paper/latex/values.tex` 新增 `valAblationOverviewRowCount=19`，并将原先残留的 `valAct...` 系列宏纳入 pipeline 生成，避免正文仍依赖手写数值。

### 3. 正文与版面调整

- `main.tex` 中的消融开头段改为引用 19 条数值对比，并说明总表最后一列指向对应细分表格。
- `main.tex` 通过 `\input{tables/ablation_overview.tex}` 读取生成式总表，正文不再维护手写总表数值。
- 为消除 layout 检查中的 underfilled page，调整 loss figure 的浮动位置和宽度，使 Loss 表、说明段和 loss figure 保持在同一页内。

## 验证结果

| 验证项 | 命令 / Job | 结果 |
| --- | --- | --- |
| Python 语法 | `python -m py_compile docs/paper/src/paper_pipeline.py` | 通过 |
| 数据重生成 | `python docs/paper/gen_data.py` | 通过；刷新 results / tables / values / TeX table；运行时仅输出 RDRND warning |
| 独立 paper-spec 扫描 | job `20260426_000806_3b5b3f8c` | passed，finding_count=0 |
| 最终 LaTeX 构建 | job `20260426_004231_e6d2ad8d`，`xelatex` 3 passes | returncode=0 |
| 最终 build 内 paper-spec | job `20260426_004232_78fcc080` | passed，finding_count=0 |
| 最终 layout-check | build job `20260426_004231_e6d2ad8d` | passed，issue_count=0 |
| 宏一致性 | Python 扫描 `main.tex` 使用的 `val...` 宏 | used=222，missing=[] |

## 修改文件

- `docs/paper/src/paper_pipeline.py`
- `docs/paper/latex/main.tex`
- `docs/paper/latex/tables/ablation_overview.tex`
- `docs/paper/data/results.json`
- `docs/paper/data/generated_tables.md`
- `docs/paper/data/values_raw.json`
- `docs/paper/latex/values.tex`
- `docs/paper/latex/build/main.pdf` 及 layout screenshot 输出由最终 LaTeX 构建刷新

## 补充执行：横评雷达子图纳入正文

- `docs/paper/latex/main.tex` 的横评小节已在 `tab:models` 后新增 `fig_02_horizontal_summary.png`，正文现在直接引用包含六指标雷达图的主横评摘要图。
- 新增 `fig:horizontal_summary`，caption 明确说明四个子图分别对应物理校准指标、Compute Cost 与 STM32F405 板端速度、六指标雷达比较和主横评收敛曲线。
- 保留 `fig_13_compensation_distribution.png` 作为后续指标分布范围补充图，避免用分布范围图替代主横评摘要图。
- 已重新执行 `python docs/paper/gen_data.py` 刷新 `fig_02_horizontal_summary.png`、`results.json`、`values.tex` 和图表 raw 记录。
- 独立 paper-spec 扫描 job `20260426_011821_eb3842e0` 通过，`finding_count=0`。
- 最终 LaTeX 构建 job `20260426_011836_6486e2d2` 通过，`returncode=0`，layout-check 通过，`issue_count=0`。
- 已将稳定规则蒸馏到 `docs/reference/paper_horizontal_comparison_method.md` 与 `docs/reference/paper_data_figure_pipeline.md`。
