# R23.4 Task Report

## 任务目标

- 修复 `Main benchmark summary` 的 (b) 面板中说明文字与柱状图/坐标元素遮挡的问题。
- 修复 `Local linear frequency responses identified at fixed input magnitudes.` 对应 `local_transfer_slices.png` 中 (a)(b) 底部 `Magnitude (m/s^2)` 标签截断与坐标标签/刻度字号严重失衡的问题。
- 将上述问题沉淀为 paper pipeline 的通用 raw 自动检测能力，避免后续图表回归。

## 实施内容

1. 新增通用视觉质量审计模块 `docs/paper/src/figure_visual_audit.py`：
   - `audit_matplotlib_figure()`：从 Matplotlib figure 中检测注释文字与数据图元重叠、坐标标签与刻度标签字号比例失衡。
   - `audit_image_file()`：从最终位图边缘检测深色非背景像素，捕获保存/拼图后的文字或图形截断。
   - `audit_montage_layout()`：检查拼图 metadata 中 panel 是否越界或相互覆盖。
   - `validate_raw_visual_quality()`：在 pipeline 末尾统一读取 raw 中的 `visual_quality_audit`，存在失败项即中断生成。

2. 接入 paper pipeline：
   - `docs/paper/src/paper_pipeline.py` 的 `_save_matplotlib_figure()` 会在保存图表时写入 `visual_quality_audit`。
   - `make_bitmap_montage()` 会同时写入 montage layout 与最终图片边缘检测结果。
   - 新增 `validate_raw_publication_quality()`，并在 `generate_all()` 中与已有 title-free raw audit 一起执行。
   - 将 `local_transfer_slices.png` 的生成接入 `generate_all()`，确保被统一 raw 检测覆盖。

3. 修复 `Main benchmark summary.(b)`：
   - 移除位于坐标轴内部的 `Higher measured throughput is better` 注释，避免文字覆盖柱状图和底部坐标元素。
   - 将“higher is better”信息合并到 y-axis label：`KEIL throughput (points/s; higher is better)`。
   - 重生成 `fig_02_horizontal_summary.png` 及其 panel/raw 文件。

4. 修复 `local_transfer_slices.png`：
   - 在 `docs/paper/src/translate_legacy_figures.py` 中降低 3D 面板 axis label 字号，统一到 11 pt；tick label 为 9 pt，raw 中记录的字号比例为 1.222，低于阈值 1.85。
   - 调整 3D 面板保存 padding、拼图 padding/gutter 与 panel trim，确保底部 `Magnitude (m/s^2)` 的右括号完整显示。
   - 为该图 raw 写入 panel-level Matplotlib audit、montage audit 与 final-image edge audit。

## 验证结果

- `python docs/paper/gen_figures.py`：通过，所有生成图和 raw 更新完成。
- `python -c "import sys; sys.path.insert(0, 'docs/paper'); from src.paper_pipeline import validate_raw_title_free, validate_raw_publication_quality; validate_raw_title_free(); validate_raw_publication_quality(); print('raw audits pass')"`：通过。
- 针对审计模块的临时探针验证：
  - 注释文字与柱状图重叠检测：`True`。
  - axis label / tick label 字号失衡检测：`True`。
  - 位图边缘截断检测：`True`。
- LaTeX 构建验证：`latex-cli` job `20260426_142105_49e8f500` 成功结束，`layout_check.status = passed`，无 layout issues。

## 关键产物

- 新增：`docs/paper/src/figure_visual_audit.py`
- 修改：`docs/paper/src/paper_pipeline.py`
- 修改：`docs/paper/src/translate_legacy_figures.py`
- 重生成：`docs/paper/figures/fig_02_horizontal_summary.png`
- 重生成：`docs/paper/figures/local_transfer_slices.png`
- 更新 raw：`docs/paper/figures/fig_02_horizontal_summary.raw.json`、`docs/paper/figures/_montage_panels/fig_02_horizontal_summary_b_throughput.raw.json`、`docs/paper/figures/local_transfer_slices.raw.json` 等。

## 结论

R23.4 点名的遮挡、截断和字号失衡问题已修复；paper pipeline 现在会将视觉质量审计写入 raw，并在生成阶段统一验证，后续若再次出现注释覆盖数据、边缘截断或坐标字号比例异常，会在 `gen_figures.py` 阶段失败并阻止问题进入论文图表产物。
## 补充：原理示意图箭头几何 raw auto check

用户追加指出代码生成的原理示意图存在箭头对不齐、箭头重叠和箭头压到图案的问题。本轮在 R23.4 通用 raw 审计基础上补齐了 schematic-level 几何检查，并修复已知示意图问题。

### 追加实施内容

1. 在 `docs/paper/src/figure_visual_audit.py` 中新增/完善 `audit_schematic_geometry()`：
   - 检查箭头端点是否落在声明的 source/target 框边界附近。
   - 检查要求轴对齐的箭头是否出现意外斜线。
   - 检查箭头是否穿过未声明为 source/target/allowed 的其他框。
   - 检查箭头之间的非端点交叉和明显共线重叠。
   - 将该检查标记为 `schematic_geometry_audit`，并纳入 `visual_quality_audit` raw 统一验证。

2. 将原理示意图的框与箭头注册到 raw 审计：
   - `fig_19_dataset_preprocessing_workflow.raw.json`
   - `fig_14_parallel_wiener_principle.raw.json`
   - `_montage_panels/fig_15_lut_lookup_principles_a_offline.raw.json`
   - `_montage_panels/fig_15_lut_lookup_principles_b_runtime.raw.json`
   - `_montage_panels/fig_17_board_inference_validation_workflow_a_export.raw.json`
   - `_montage_panels/fig_17_board_inference_validation_workflow_b_validate.raw.json`

3. 修复 `Parallel Wiener equivalent modeling of the amplitude-dependent MET response.` 的 (a) 面板：
   - 移除支路输出侧的 merge bus、竖线和虚线路径，改为三个 `f_i(.)` 分支直接向 SUM 节点聚合。
   - 将输入分流、分支内部连接、分支到 SUM、SUM 到输出的箭头统一缩短为原始连接长度的 2/3。
   - 将 SUM 圆形节点注册为受保护 schematic box，使 raw 审计能发现箭头压到求和符号的回归。
   - 额外输出独立单图 `_montage_panels/fig_22_parallel_wiener_equivalent_montage_a_principle.png`，并同步复制为 `fig_14_parallel_wiener_principle.png`，便于单独审阅。

4. 对 LUT、dataset workflow、board inference workflow 等原理示意图补充图案化表达，同时对对应箭头路径加入 raw geometry 审计，避免纯文本框和箭头重叠问题反复出现。

### 追加验证结果

- `python -m py_compile docs/paper/src/paper_pipeline.py docs/paper/src/figure_visual_audit.py docs/paper/src/translate_legacy_figures.py`：通过。
- `python docs/paper/gen_figures.py`：通过。
- `validate_raw_title_free()` + `validate_raw_publication_quality()`：通过。
- schematic 几何 raw 审计逐项通过：
  - dataset preprocessing workflow：7 boxes / 6 arrows。
  - parallel Wiener principle：7 boxes / 11 arrows，所有记录箭头长度均为 full connector 的 0.666667 附近并低于 0.7 阈值。
  - LUT offline construction：3 boxes / 2 arrows。
  - LUT runtime lookup：5 boxes / 5 arrows。
  - board inference export：3 boxes / 2 arrows。
  - board inference validation：5 boxes / 4 arrows。
- LaTeX 构建验证：`latex-cli` job `20260426_231406_a7521aac` 成功结束，`returncode = 0`，`paper_spec_scan.result.passed = true`，`layout_check.status = passed`。

