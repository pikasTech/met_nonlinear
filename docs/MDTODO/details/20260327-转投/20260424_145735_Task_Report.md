# R19.1 Task Report

## Completed Scope

- Distilled the R19 paper data/figure architecture into long-term docs:
  - `docs/reference/paper_data_figure_pipeline.md`
  - updated `docs/reference/paper_latex_submission.md`
  - updated the paper index in `AGENTS.md`
- Migrated legacy bibliography:
  - `C:/work/met_nonlinear_paper/nonlinear.bib` -> `docs/paper/latex/nonlinear.bib`
  - copied `docs/paper/latex/bst/sn-nature.bst` to `docs/paper/latex/sn-nature.bst` so BibTeX can resolve the Springer style from the manuscript working directory.
- Migrated legacy images:
  - `C:/work/met_nonlinear_paper/image/` -> `docs/paper/image/`
  - selected legacy paper images are copied into `docs/paper/figures/legacy_*.png` through the generation pipeline.
- Migrated legacy plotting code for provenance:
  - `C:/work/met_nonlinear_paper/figure_paper.py` -> `docs/paper/src/legacy/figure_paper.py`
- Integrated the migration into the R19 entrypoint architecture:
  - `docs/paper/config.json` now records `legacy_image_dir`, `legacy_image_migrations`, and `legacy_figure_code`.
  - `docs/paper/src/paper_pipeline.py` now refreshes generated figures, distinct paper figures, and migrated legacy image figures through `gen_figures.py`.
- Replaced inline placeholder bibliography in `main.tex` with real BibTeX bibliography usage:
  - `\bibliography{nonlinear}`
  - citations that were missing in the migrated `.bib` were remapped to real keys already present in `nonlinear.bib`.

## Bibliography Notes

- Deduplicated one repeated BibTeX entry key: `WOS:001409548400009`.
- Removed fragile URL fields from the migrated `.bib` where they caused local XeLaTeX/BibTeX failures under the current Springer style.
- Replaced the R-Sensors `howpublished` URL command with plain text to avoid `breakurl`/XeLaTeX `\pdf@box` errors.
- Verified all citation keys used by `main.tex` exist in `docs/paper/latex/nonlinear.bib`.
- Generated `docs/paper/latex/build/main.bbl` from the real migrated bibliography.

## Commands Run

```powershell
python docs/paper/gen_data.py
python docs/paper/gen_figures.py
xelatex -interaction=nonstopmode -file-line-error -output-directory=build main.tex
bibtex build/main
xelatex -interaction=nonstopmode -file-line-error -output-directory=build main.tex
xelatex -interaction=nonstopmode -file-line-error -output-directory=build main.tex
python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir C:/work/met_nonlinear_master/docs/paper/latex --tex main.tex --engine xelatex --passes 2
```

Final LaTeX skill build:

- job_id: `20260424_151653_bbc525a0`
- status: `done`
- returncode: `0`
- PDF: `docs/paper/latex/build/main.pdf`

## Consistency Checks

- No duplicate `\includegraphics` targets in `main.tex`.
- All `main.tex` figure paths resolve.
- Every referenced `docs/paper/figures/*.png` has a same-stem `.raw.json`.
- Every `\val...` macro used by `main.tex` is defined in `values.tex`.
- No duplicate value macro definitions and no `TBD` remains in `values.tex`.
- No inline placeholder `Reference record` bibliography remains in `main.tex` or `build/main.bbl`.
- New long-term reference doc was re-read as UTF-8 and contains no replacement-character or question-mark corruption.
