# R24.4.1 任务报告

## 任务目标

根据 R24.4 的排查结论，在正文中补充 `Table 5` 约束消融的解释，明确 `Odd / No positive basis` 对照为什么会出现明显退化，并将该现象收束为 Baseline 结构优势的论述。

## 修改内容

- 修改 `docs/paper/latex/main.tex` 中 `Physical sign and symmetry constraints` 小节的约束消融说明。
- 在原有“Odd-only 分支出现明显指标放大”基础上，补充三层语义：
  - `Odd / No positive basis` 保留奇对称性，但移除了正输入区间的方向保持约束；
  - 该对照更容易偏离稳定补偿映射，因此训练稳定性更差；
  - Baseline 同时保留奇对称性与正值性，使非线性补偿映射更符合器件响应规律，因此三项物理指标的收敛更稳定。

## 写作口径调整

本次没有改动表格数据、pipeline 或实验配置，只补充正文解释，并保持与 R24.4 的代码级调查结论一致：

- 不把该对照写成“完全无约束”；
- 直接把退化原因落在正值约束缺失带来的稳定性下降；
- 将结论表述为 Baseline 的结构先验优势，而不是实现错误。

## 结果检查

- 约束消融段落现在已明确区分“奇对称性”和“正值性”的作用。
- `Odd / No positive basis` 的退化被解释为稳定补偿映射先验减弱后的结果。
- Baseline 优势被收束为“同时保留奇对称性与正值性带来更稳定收敛”，与表中三项物理指标方向一致。

## 验证

- `python C:/Users/lyon/.agents/skills/paper-spec/scripts/paper-spec-cli.py scan --path docs/paper/latex/main.tex`：通过，`finding_count = 0`。
- `python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py debug probe --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex`：通过。
- `python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex`：成功，job `20260427_163242_69e576d4` 返回 `status = done`、`returncode = 0`，`layout_check.status = passed`、`issue_count = 0`。
