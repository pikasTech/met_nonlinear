# R23.5.1 Task Report

## 任务

将 `Embedded inference validation workflow and performance` 的 (c) 面板调整为以 RAM 为横轴、板端性能为纵轴、MAE 为颜色编码，并重新编译 LaTeX 确认论文构建通过。

## 修改内容

- 更新 `docs/paper/src/paper_pipeline.py` 中 `make_onboard_figure()` 的 (c) 面板绘图逻辑：
  - 横轴为 `RAM usage (KB)`。
  - 纵轴为 `KEIL throughput (points/s; higher is better)`，采用对数坐标展示 `No LUT exact` 到 LUT 查表实现之间的吞吐跨度。
  - 颜色编码为 `KEIL-MAE against TensorFlow`，采用 `LogNorm` 展示不同实现的 C 端数值误差差异。
  - 保留圆形/菱形 marker 区分 `Baseline export` 与 `Wiener-KAN variant`。
  - 在 raw payload 中写入 `axis_encoding`：`x=RAM usage (KB)`、`y=KEIL throughput (points/s)`、`color=KEIL-MAE against TensorFlow`。
- 更新 `docs/paper/latex/main.tex` 中 Fig. 12 caption 与正文描述：
  - caption 说明 marker position 表示 RAM usage 与 measured KEIL throughput，marker color 表示 KEIL-MAE。
  - 正文对应描述改为“RAM--吞吐率--KEIL-MAE 关系”。
- 重新执行论文数据/图生成：
  - 刷新 `docs/paper/figures/fig_05_onboard_inference.png` 与 raw JSON。
  - 同步刷新 `docs/paper/data/results.json`、`docs/paper/data/generated_tables.md` 和 `docs/paper/latex/values.tex`，保持 paper pipeline 输出一致。

## 验证

- `python -m py_compile docs\paper\src\paper_pipeline.py`：通过。
- `python docs\paper\gen_data.py`：通过，已重新生成论文数据、图和 TeX 宏；命令末尾出现 CPU RDRND warning，但进程返回码为 0，未影响生成结果。
- raw JSON 抽查：`docs/paper/figures/_montage_panels/fig_05_onboard_inference_c_embedded_performance.raw.json` 中 `axis_encoding` 为 `x=RAM usage (KB)`、`y=KEIL throughput (points/s)`、`color=KEIL-MAE against TensorFlow`。
- `latex-cli.py build --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex --engine xelatex --passes 2`：通过。
  - job_id: `20260427_002733_b778109b`
  - XeLaTeX returncode: `0`
  - `paper_spec_scan.passed=true`，未发现正文泄露/禁用表达问题。
  - `layout_check.status=passed`，`issue_count=0`。
  - 输出 PDF：`docs/paper/latex/build/main.pdf`，共 28 页。
- `pdftotext docs\paper\latex\build\main.pdf -` 抽查：PDF 中 Fig. 12 caption 已包含 “marker position reports RAM usage and measured KEIL throughput, while marker color encodes KEIL-MAE”，正文已包含 “RAM--吞吐率--KEIL-MAE 关系”。

## 结论

R23.5.1 已按更正口径完成。(c) 面板现在以 RAM usage 作为横轴、KEIL throughput (points/s) 作为纵轴、KEIL-MAE 作为颜色编码，正文和 caption 已同步更新，LaTeX 编译和 post-build layout 检查均通过。
