# R23.2.2 Task Report

## 任务目标

1. 恢复 Table 5 中不同 Group 之间的虚线分隔。
2. 从 Table 5 中移除全部 Sensitivity Group 行。

## 修改内容

- 在 `docs/paper/src/paper_pipeline.py` 中调整 `build_ablation_overview_rows()`：Table 5 的 overview 数据只保留 `Mapping`、`Constr.`、`Wiener front` 和 `Loss` 四组，删除原先由 hyperparameter sensitivity 汇入 Table 5 的 `Sensitivity` 组。
- 在 `write_ablation_overview_tex()` 中按 group 边界自动插入 `\hdashline`，并将 `\multirow` 宽度改为 `=`，使分组单元格按列宽排版并避免表格 overfull。
- 将 Table 5 表头从 `Variant / axis` 改为 `Variant`，与删除 sensitivity axis 后的内容保持一致。
- 更新 `docs/paper/latex/main.tex` 中 Table 5 的引导语和 caption：总表仅描述 design contrast，不再声称 Table 5 覆盖 hyperparameter sensitivity；超参数敏感性结果继续由 `Fig.~\ref{fig:hparam_sensitivity}` 承载。
- 重新执行 `python docs/paper/gen_data.py`，刷新 `docs/paper/data/results.json`、`docs/paper/data/generated_tables.md`、`docs/paper/latex/values.tex` 与 `docs/paper/latex/tables/ablation_overview.tex`。

## 结果检查

- `docs/paper/latex/tables/ablation_overview.tex` 中包含 3 条 `\hdashline`，分别位于 `Mapping`/`Constr.`、`Constr.`/`Wiener front`、`Wiener front`/`Loss` 之间。
- `docs/paper/data/results.json` 中 `ablation_overview` 行数为 13，Group 集合为 `Constr.`、`Loss`、`Mapping`、`Wiener front`，不存在 `Sensitivity` group。
- `docs/paper/latex/values.tex` 中 `\valAblationOverviewRowCount` 更新为 `13`。
- 编译截图检查 `docs/paper/latex/build/main/screenshots/page-23.png`：Table 5 可见组间虚线，且已无 Sensitivity Group。

## 验证命令

- `python docs/paper/gen_data.py`：成功。
- `python C:/Users/lyon/.agents/skills/paper-spec/scripts/paper-spec-cli.py scan --path docs/paper/latex/main.tex --max-findings 100`：通过，job `20260426_231545_2914b96e`，finding_count = 0。
- `xelatex -interaction=nonstopmode -file-line-error -output-directory=build -output-driver="xdvipdfmx -C 0x0040" main.tex`：连续两轮成功，输出 `docs/paper/latex/build/main.pdf`，29 页。
- `pdfinfo docs/paper/latex/build/main.pdf`：PDF 可解析，29 页。
- `python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py layout-check --pdf c:/work/met_nonlinear_master/docs/paper/latex/build/main.pdf --tex c:/work/met_nonlinear_master/docs/paper/latex/main.tex --log c:/work/met_nonlinear_master/docs/paper/latex/build/main.log`：通过，issue_count = 0。
