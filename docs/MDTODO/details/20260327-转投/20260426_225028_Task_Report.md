# R23.5 Task Report

## 任务

将原先的 Embedded inference validation workflow 与 Embedded inference performance 合并为一张图；移除性能图中单独的吞吐率、RAM 两个柱状子图，并把横向模型和 Wiener-KAN 的三种实现变体统一放入性能子图中。

## 修改内容

- 重构 `docs/paper/src/paper_pipeline.py` 中的 `make_onboard_figure()`：
  - 复用 C export workflow 与 QEMU/STM32F405 validation workflow 两个面板作为新图的 (a)、(b)。
  - 删除原 Fig. 13(a) throughput 与 Fig. 13(b) RAM 的柱状图生成路径。
  - 新增 Fig. 12(c) 的综合性能散点图，将 TCN、1DCNN、LSTM、LSTMTransformer、RNN、GRU 与 Wiener-KAN (LUT nearest / LUT interp / No LUT) 放在同一 RAM--KEIL-MAE 坐标中，颜色和点大小编码 KEIL throughput。
  - 在 raw JSON 中写入 `performance_rows`，保留图数据溯源。
- 更新 `docs/paper/latex/main.tex`：
  - 将原独立 workflow 图和 performance 图合并为一个 `fig_05_onboard_inference.png` 引用。
  - 更新 caption 为 “Embedded inference validation workflow and performance”。
  - 更新正文描述，使 Fig. 12(a)--(b) 对应验证流程，Fig. 12(c) 对应横向模型和三种 Wiener-KAN 导出实现。
  - 在 LUT 变体表中补入 `No LUT exact` 行。
- 更新 `docs/paper/latex/values.tex` 的生成宏：
  - 新增 `valDeployLutNearest*`、`valDeployLutInterp*`、`valDeployNoLut*` 三组宏，供正文表格引用。
- 重新生成论文数据和图：
  - `docs/paper/figures/fig_05_onboard_inference.png`
  - `docs/paper/figures/fig_05_onboard_inference.raw.json`
  - `docs/paper/data/results.json`
  - `docs/paper/data/values_raw.json`
  - `docs/paper/data/generated_tables.md`
  - `docs/paper/latex/values.tex`

## 验证

- `python -m py_compile docs/paper/src/paper_pipeline.py`：通过。
- `python docs/paper/src/paper_pipeline.py`：通过，已重新生成图、数据和 TeX 宏。
- `paper-spec-cli.py scan --path docs/paper/latex/main.tex`：通过，未发现正文泄露/禁用表达问题。
- `latex-cli.py build --workdir docs/paper/latex --tex main.tex --engine xelatex --passes 2`：XeLaTeX 返回码 0，生成 `docs/paper/latex/build/main.pdf`。
- `pdftotext docs/paper/latex/build/main.pdf -` 抽查：PDF 中 Fig. 12 caption 已更新为合并后的 “Embedded inference validation workflow and performance”，正文引用更新为 Fig. 12(a)--(b) 与 Fig. 12(c)。

## 备注

- latex skill 的 post-build layout-check 使用 `pdftoppm` 导出页面截图时出现 Poppler 渲染报错，并将部分页面误判为空白；但 XeLaTeX 编译返回码为 0，`pypdf` 与 `pdftotext` 均可读取对应页面文本。该问题记录为渲染器侧残留风险，不影响本次 R23.5 的图合并和引用更新结论。
