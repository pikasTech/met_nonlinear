# R14.1 任务报告

- 时间：2026-04-24
- 任务：将 R14 的非线性理论分析与并联 Wiener 专题稿合并到 `docs/paper/latex/main.tex`，并整合对应图表、数据宏与 LaTeX 编译验证。
- 结论：已完成合并。全文通读 `main.tex` 后，采用“Introduction 提前交代理论机理，Background 的测量结果后接并联 Wiener 等效建模”的结构，使正文形成“物理机理 -> 实测频响漂移 -> 并联 Wiener 等效解释 -> Wiener-KAN 方法设计”的连续叙事链条。

## 1. 全文结构判断

按用户建议并结合全文结构，最终采用如下落点：

1. `Introduction` 第一段后补入 MET 大信号非线性的物理来源，包括机械几何非线性、流体惯性非线性、离子输运乘法耦合和电极反应动力学；
2. `Background and Principles / Nonlinear Frequency Response Characteristics of MET` 中，先保留原有不同震级下频响漂移的测量图，再在测量结果后引入三支路并联 Wiener 等效模型；
3. `Proposed Method` 保持聚焦 Wiener-KAN 本体，避免把上游等效建模与 LRNN/IIR 初始化推导混在一起。

该组织方式比直接把全部内容塞入方法章节更清晰：理论机理用于定义问题，等效模型用于解释测量现象，Wiener-KAN 则作为后续补偿结构自然承接。

## 2. 正文整合

更新文件：

- `docs/paper/latex/main.tex`

主要修改：

- 在 `Introduction` 中补入大信号非线性的四类物理来源；
- 在 `Nonlinear Frequency Response Characteristics of MET` 的测量结果图之后，补入三支路并联 Wiener 等效模型；
- 新增并联 Wiener 等效模型公式：
  - `eq:parallel_wiener_equivalent`
  - `eq:parallel_wiener_branch`
- 新增并联 Wiener 拟合结果图：
  - `fig:parallel_wiener_modeling`
- 在正文中引用自动生成的数据宏：
  - `\valWienerParallelAmpCount`
  - `\valWienerParallelCfMae`
  - `\valWienerParallelGainFreq`
  - `\valWienerParallelGainMae`

## 3. 图表与数据整合

更新文件：

- `docs/paper/src/paper_pipeline.py`
- `docs/paper/data/results.json`
- `docs/paper/latex/values.tex`

新增/刷新图表产物：

- `docs/paper/figures/fig_14_parallel_wiener_response.png`
- `docs/paper/figures/fig_14_parallel_wiener_response.raw.json`
- `docs/paper/figures/fig_14_parallel_wiener_branch_weights.png`
- `docs/paper/figures/fig_14_parallel_wiener_branch_weights.raw.json`

数据来源：

- `ex_projects/compare/wiener_parallel_modeling/data/wiener_parallel_modeling_summary.json`
- `ex_projects/compare/wiener_parallel_modeling/image/14.NN_extern_simu_reproduced_analysis.png`
- `ex_projects/compare/wiener_parallel_modeling/image/14.NN_extern_simu_reproduced_fh_kx.png`

生成的数据宏包括：

- `\valWienerParallelAmpCount = 25`
- `\valWienerParallelCfMae = 1.87`
- `\valWienerParallelCfRmse = 2.34`
- `\valWienerParallelGainFreq = 100`
- `\valWienerParallelGainMae = 4.53`
- `\valWienerParallelGainRmse = 5.53`

## 4. 验证

### 数据与图表生成

执行：

```bash
python docs/paper/gen_data.py
```

结果：成功刷新 `docs/paper/data/results.json`、`docs/paper/latex/values.tex` 以及新增的 `fig_14_*` 图表产物。运行时出现 CPU RDRND warning，但命令返回码为 0，图表和数据文件已生成。

### LaTeX 编译

执行：

```bash
python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex --engine xelatex --passes 2
```

结果：

- Job ID：`20260424_173438_cdae9ccf`
- 状态：`done`
- 返回码：`0`
- 输出 PDF：`docs/paper/latex/build/main.pdf`
- 编译页数：`29 pages`

### 文本检查

执行了关键字检查，确认新增并联 Wiener 公式、图引用和数据宏均已落入 `main.tex` / `values.tex` / `results.json`。同时检查 `???` 乱码模式，未发现新增乱码。

## 5. 任务结论

R14.1 已完成。当前总稿已经吸收 R14 的理论分析和并联 Wiener 建模结果，并且图表、数据宏和 LaTeX 编译链路均已验证通过。最终结构采用用户建议的布局：理论分析进入 Introduction，等效建模放在测量结果之后并靠近 Introduction，使正文叙事从问题机理自然过渡到补偿方法。

## 6. 追加：机理与建模原理示意图

根据后续要求，已在全文复读 `docs/paper/latex/main.tex` 后补充两张原理示意图，并插入正文：

- `docs/paper/figures/fig_14_met_nonlinear_mechanism.png`：MET 大信号非线性机理示意图，插入 `Introduction` 中理论机理段落之后；
- `docs/paper/figures/fig_14_parallel_wiener_principle.png`：三支路并联 Wiener 等效建模原理图，插入测量结果之后、并联 Wiener 公式之前；
- 同步生成 `.raw.json`：`fig_14_met_nonlinear_mechanism.raw.json` 与 `fig_14_parallel_wiener_principle.raw.json`；
- 图生成逻辑已纳入 `docs/paper/src/paper_pipeline.py`，并已刷新 `docs/paper/data/results.json`。

验证：

```bash
python docs/paper/gen_data.py
python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex --engine xelatex --passes 2
```

结果：LaTeX Job `20260424_175623_12b00794` 完成，返回码 `0`，输出 `docs/paper/latex/build/main.pdf`，共 `29 pages`。

## 7. 追加：机理公式位置修正

根据进一步反馈，已再次全文读取 `docs/paper/latex/main.tex` 后调整机理内容位置：

- 从 `Introduction` 移除机理示意图，避免图在引言中过于概念化；
- 将机理示意图移动到 `Background and Principles` 的物理控制方程附近；
- 在 `fun:h_mech_model` 与 `eq:navier_stokes` 之后补入大信号非线性机理公式 `eq:nonlinear_mechanism_terms`；
- 公式显式概括四类物理来源：橡胶膜等效刚度随幅值变化、流体惯性项、离子通量乘法耦合、Butler--Volmer 电极反应电流；
- 正文说明这些项会改变等效 `\fn`、`\Sen` 和 `\zeta`，从而产生幅值依赖的局部频率响应迁移。

验证：

```bash
python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex --engine xelatex --passes 2
```

结果：LaTeX Job `20260424_181016_231109c4` 完成，返回码 `0`，输出 `docs/paper/latex/build/main.pdf`，共 `29 pages`。

## 8. 追加：重绘物理化学机理示意图

根据进一步反馈，原机理图过于接近纯框图，已重绘为包含明确物理化学要素的示意图：

- 左侧加入橡胶膜/质量块和大位移变形，标注 `k(A)=k_0+k_2A^2`；
- 中央加入电解液微通道、阳极/阴极、电解液流线、涡旋惯性、`I^-` / `I_3^-` 离子图案；
- 图中显式标注离子通量 `J=-D∇c+cu`、流体惯性 `ρ(u·∇)u` 与 Butler--Volmer 电极动力学；
- 右侧汇总到等效响应参数 `\eta(A)`、`S(A)`、`\zeta(A)`，再连接到频率响应漂移；
- 图生成逻辑仍在 `docs/paper/src/paper_pipeline.py`，产物仍为 `docs/paper/figures/fig_14_met_nonlinear_mechanism.png`，并刷新 `.raw.json` 与 `docs/paper/data/results.json`。

验证：

```bash
python docs/paper/gen_data.py
python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex --engine xelatex --passes 2
```

结果：LaTeX Job `20260424_182502_4c067a78` 完成，返回码 `0`，输出 `docs/paper/latex/build/main.pdf`，共 `27 pages`。

## 9. 追加：改用 imagegen 生成物理化学机理图

根据进一步反馈，已改用 `imagegen` 生成更接近论文插图风格的机理图，并替换原 matplotlib 绘制版本：

- 原始生成图保留在 Codex 默认生成目录；
- 可复现项目资产复制到 `docs/paper/assets/fig_14_met_nonlinear_mechanism_ai.png`；
- 论文引用图复制到 `docs/paper/figures/fig_14_met_nonlinear_mechanism.png`；
- `docs/paper/src/paper_pipeline.py` 已改为从 `docs/paper/assets/fig_14_met_nonlinear_mechanism_ai.png` 复制到 figures，避免后续 `gen_data.py` 覆盖回代码框图版本；
- `.raw.json` 记录该图为 AI-rendered scientific schematic，并列出膜片大变形、电解液微通道、阳极/阴极、碘离子/三碘离子输运、电极反应和频响漂移等要素。

新版图已包含：橡胶膜大挠度、微通道流线和涡旋、I-/I3- 离子迁移、阳极/阴极多孔电极、redox kinetics、电路电压读出以及频率响应曲线随幅值右移和增益变化。

验证：

```bash
python docs/paper/gen_data.py
python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex --engine xelatex --passes 2
```

结果：LaTeX Job `20260424_184451_55a11d3a` 完成，返回码 `0`，输出 `docs/paper/latex/build/main.pdf`，共 `27 pages`。

## 10. 追加：按 MET 结构示意图风格重绘二维机理图

根据进一步反馈，已重新用 `imagegen` 生成二维、简单上色、接近既有 `legacy_4_MET_structure.png` 风格的机理图：

- 图像以白底、黑色轮廓、浅青色电解液填充为主；
- 保留上下橡胶膜、支架、电解液区域和中心敏感电极对等结构示意元素；
- 原电池/电极反应部分简化为中心电极附近的局部 redox kinetics 示意，不再绘制完整复杂原电池；
- 保留必要物理机制：膜片大挠度、非线性流动涡旋、I-/I3- 离子输运、浓度梯度、频响漂移小插图；
- 新图已替换 `docs/paper/assets/fig_14_met_nonlinear_mechanism_ai.png` 与 `docs/paper/figures/fig_14_met_nonlinear_mechanism.png`，并通过 `docs/paper/src/paper_pipeline.py` 继续作为可复现资产复制。

验证：

```bash
python docs/paper/gen_data.py
python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex --engine xelatex --passes 2
```

结果：LaTeX Job `20260424_185408_e2427e66` 完成，返回码 `0`，输出 `docs/paper/latex/build/main.pdf`，共 `27 pages`。

## 11. 追加：突出频率特性漂移关键因素

根据进一步反馈，上一版二维图更多体现正常工作过程，关键漂移因素不够突出。已重新用 `imagegen` 生成强调“频率特性漂移原因”的版本，并替换当前论文图：

- 显式拆出四个导致频响漂移的机制框：
  1. Membrane large deflection -> effective stiffness increases -> natural frequency `η(A)` shifts；
  2. Fluid inertia / hydrodynamic resistance -> damping `ζ(A)` changes；
  3. Concentration polarization / ion transport nonuniformity -> nonuniform ion flux；
  4. Electrode reaction nonlinearity -> Butler--Volmer redox nonlinearity -> sensitivity `S(A)` drifts；
- 将四个机制共同连接到 `local transfer function H(s; A) changes`，并输出 `η(A)`、`S(A)`、`ζ(A)`；
- 右侧继续保留频率响应曲线右移和增益变化的小图；
- 左侧仍维持与 `legacy_4_MET_structure.png` 相近的二维反应腔结构风格。

更新文件：

- `docs/paper/assets/fig_14_met_nonlinear_mechanism_ai.png`
- `docs/paper/figures/fig_14_met_nonlinear_mechanism.png`
- `docs/paper/data/results.json`
- `docs/paper/figures/fig_14_met_nonlinear_mechanism.raw.json`

验证：

```bash
python docs/paper/gen_data.py
python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex --engine xelatex --passes 2
```

结果：LaTeX Job `20260424_190010_42448fd9` 完成，返回码 `0`，输出 `docs/paper/latex/build/main.pdf`，共 `27 pages`。

## 12. 追加：进一步简化左侧结构以突出原理

根据进一步反馈，上一版左侧整体结构仍与已有结构示意图重复。已重新生成更简化的原理图：

- 移除外壳、支架、输出线等完整传感器结构元素；
- 左侧只保留上下橡胶膜、电解液区域、局部敏感电极对和输入加速度；
- 保留四个漂移机制：刚度上升、惯性流/水动力阻力、浓度极化/非均匀离子通量、Butler--Volmer redox 非线性；
- 继续将四类机制汇总到 `H(s; A)`、`η(A)`、`S(A)`、`ζ(A)`，再连接频响漂移小图；
- 当前图更聚焦原理，避免与 `legacy_4_MET_structure.png` 的完整结构说明重复。

更新文件：

- `docs/paper/assets/fig_14_met_nonlinear_mechanism_ai.png`
- `docs/paper/figures/fig_14_met_nonlinear_mechanism.png`
- `docs/paper/data/results.json`

验证：

```bash
python docs/paper/gen_data.py
python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex --engine xelatex --passes 2
```

结果：LaTeX Job `20260424_190935_3d514ddd` 完成，返回码 `0`，输出 `docs/paper/latex/build/main.pdf`，共 `27 pages`。

## 13. 追加：AI 示意图经验固化与 LUT/板端流程图补充

按 `docs-spec` 要求，已将本轮 AI 论文示意图绘制经验沉淀为长期参考文档：

- `docs/reference/paper_ai_schematic_figures.md`
- `AGENTS.md` 已增加该参考文档索引。

该文档固化了以下经验：AI 图适用于物理机理、部署原理和流程架构示意；定量图仍应使用代码生成；AI 图应先进入 `docs/paper/assets/`，再由 `docs/paper/src/paper_pipeline.py` 复制到 `docs/paper/figures/`；机理图应表达“原因 -> 参数变化 -> 观测现象”，避免只画正常工作过程或重复已有结构图。

同时补充三张部署相关原理/流程图：

- `docs/paper/assets/fig_15_lut_nearest_principle_ai.png` -> `docs/paper/figures/fig_15_lut_nearest_principle.png`
- `docs/paper/assets/fig_16_lut_linear_interpolation_principle_ai.png` -> `docs/paper/figures/fig_16_lut_linear_interpolation_principle.png`
- `docs/paper/assets/fig_17_board_inference_validation_workflow_ai.png` -> `docs/paper/figures/fig_17_board_inference_validation_workflow.png`

正文更新：

- 在 `docs/paper/latex/main.tex` 的嵌入式部署段落加入板端验证流程图 `fig:board_inference_validation_workflow`；
- 加入最近邻 LUT 原理图 `fig:lut_nearest_principle`；
- 加入 LUT + 一阶线性插值原理图 `fig:lut_linear_interpolation_principle`；
- 正文补充 QEMU/Keil 验证路径、数据归一化与 C 数组打包、QEMU-MAE/KEIL-MAE、UART benchmark loop、Flash/RAM 统计等说明。

验证：

```bash
python docs/paper/gen_data.py
python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir c:/work/met_nonlinear_master/docs/paper/latex --tex main.tex --engine xelatex --passes 2
```

结果：LaTeX Job `20260424_192911_3e091877` 完成，返回码 `0`，输出 `docs/paper/latex/build/main.pdf`，共 `31 pages`。第一遍编译出现新增浮动图引用的正常 rerun warning，第二遍已通过。

## 14. 追加：AFMAE、数据集流程图与 LUT 双面板合并

- 按用户选择 `1,2,4` 补充了 AFMAE 损失原理图和数据集/预处理流程图，并将原有 LUT 最近邻与 LUT+线性插值两张图合并为一张双面板图。
- 新增资产：`docs/paper/assets/fig_18_afmae_loss_principle_ai.png`、`docs/paper/assets/fig_19_dataset_preprocessing_workflow_ai.png`、`docs/paper/assets/fig_15_lut_lookup_principles_ai.png`。
- 流水线更新：`docs/paper/src/paper_pipeline.py` 现在会复制 `fig_18_afmae_loss_principle.png`、`fig_19_dataset_preprocessing_workflow.png` 和 `fig_15_lut_lookup_principles.png`，并使用合并后的 LUT 图代替两张独立 LUT 图。
- 正文插入位置：AFMAE 图放在 `AFMAE Loss for Amplitude-Frequency Response` 推导前，数据集图放在 `Dataset and Training` 的数据集定义与预处理描述后，LUT 合并图放在板端验证流程图后。标签分别为 `fig:afmae_loss_principle`、`fig:dataset_preprocessing_workflow`、`fig:lut_lookup_principles`。
- 验证：执行 `python docs/paper/gen_data.py` 成功；XeLaTeX 编译 job `20260424_195225_c3f4ce20` 返回码 0，输出 `docs/paper/latex/build/main.pdf`，共 32 页。
