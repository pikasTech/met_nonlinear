# R22 Task Report

## 完成内容

- 新增 `src/visualization/subfigure_montage.py`，提供通用位图子图拼接模块，支持水平、垂直和矩阵布局，并支持每个子图独立设置缩放、适配宽高、水平/垂直对齐、白边裁剪、子图标签和输出 DPI。
- 在 `docs/paper/src/paper_pipeline.py` 中集成拼图流程，生成四个论文用拼图产物及相邻 `.raw.json` 元数据：
  - `docs/paper/figures/fig_20_met_structure_readout_montage.png`
  - `docs/paper/figures/fig_21_experimental_setup_dataset_workflow_montage.png`
  - `docs/paper/figures/fig_22_parallel_wiener_equivalent_montage.png`
  - `docs/paper/figures/fig_23_kan_neuron_compensation_montage.png`
- 更新 `docs/paper/latex/main.tex`，将 MET 结构/读出电路、实验装置/数据集流程、并联 Wiener 原理/响应结果改为组合图引用，并更新正文中的 `(a)`、`(b)` 指向；KAN 单神经元补偿图切换到由同一拼图模块归一化输出的位图。
- 实际读取生成后的拼图和 PDF 页面截图，调整了白边裁剪、标签边距和 LaTeX 显示宽度，使标签不覆盖标题和图内元素。

## 验证记录

- `python -m compileall src/visualization/subfigure_montage.py docs/paper/src/paper_pipeline.py` 通过。
- `python docs/paper/gen_figures.py` 通过，已重新生成 paper pipeline 产物、`docs/paper/data/results.json` 和 `docs/paper/latex/values.tex`。
- LaTeX 构建：`python C:/Users/lyon/.agents/skills/latex/scripts/latex-cli.py build --workdir "C:/work/met_nonlinear_master/docs/paper/latex" --tex main.tex --engine xelatex --passes 2` 成功返回，paper-spec 扫描通过。
- 构建完成且 `xelatex` / `xdvipdfmx` 子进程结束后，重新执行 `layout-check`：`status=passed`，`issue_count=0`。

## 备注

- LaTeX skill 的 build 状态里曾在 `xdvipdfmx` 子进程尚未完全退出时记录一次临时 layout-check 失败；等待子进程结束后 PDF 完整落盘，独立重跑 layout-check 已通过。
