# R24.10 任务报告：Fig. 10 拼图扩展与 Fig. 14 子图编号修正

## 执行范围

- 修改 `src/visualization/subfigure_montage.py`，为论文位图拼图补充跨列 `col_span` 能力，用于让单个子图独占整行两列。
- 修改 `docs/paper/src/paper_pipeline.py`，将 `time_domain_outputs.png` 并入 `fig_08_frequency_response_comparison.png` 的第三行，并去掉 `fig_18_hparam_sensitivity.png` 中 legend 面板的编号，同时把第三行 loss 子图改为从 `(f)` 开始编号。
- 修改 `docs/paper/latex/main.tex`，把原独立的时域图合并进 Fig. 10 的 caption 与正文引用，并同步更新超参数敏感性图的 caption 和正文引用编号。
- 重新生成与本任务直接相关的论文图、traceability raw 文件，以及由 paper pipeline 刷新的结果与 `values.tex`。

## 完成内容

### 1. Fig. 10 合并原 Fig. 11 的时域图

- `fig_08_frequency_response_comparison.png` 已从原来的 `2 x 2` 拼图扩展为 `3 x 2` 布局。
- 新增 `(e)` 面板，对应原独立图 `Time-domain outputs of the Wiener-KAN compensator under different frequency and magnitude conditions.`。
- `(e)` 面板通过跨列拼图方式独占第三行的第 1、2 列，满足 R24.10 对版式的要求。
- `main.tex` 中原单独 `figure` 环境已删除，Fig. 10 的 caption 与正文现在直接包含 `(e)` 面板说明与引用。

### 2. Fig. 14 的 legend 编号修正

- `fig_18_hparam_sensitivity.png` 的第二行中间 legend 面板已去掉 `(f)` 编号。
- 第三行三个 loss 收敛子图的编号已调整为 `(f)`、`(g)`、`(h)`。
- `main.tex` 中该图的 caption 已改为：第二行中间 legend 面板无编号，并把 loss 子图引用改为 `(f)--(h)`。
- 正文中原 `Fig.~\ref{fig:hparam_sensitivity}(a)--(f)` 和 `Fig.~\ref{fig:hparam_sensitivity}(g)--(i)` 的表述已同步修正。

## 验证结果

| 验证项 | 命令 / Job | 结果 |
| --- | --- | --- |
| Python 语法 | `python -m compileall src/visualization/subfigure_montage.py docs/paper/src/paper_pipeline.py` | 通过 |
| 论文数据/图重生成 | `python docs/paper/src/paper_pipeline.py` | 通过；刷新 Fig. 10 / Fig. 14 对应拼图与相关 raw/json 产物 |
| LaTeX probe | `latex-cli.py debug probe --workdir docs/paper/latex --tex main.tex` | 通过；解析到 `xelatex` |
| 最终 LaTeX 构建 | job `20260427_171612_71c9e340` | `returncode=0`，`main.pdf` 已刷新 |
| build 内 paper-spec 扫描 | job `20260427_171612_71c9e340` | `passed=true`，`finding_count=0` |
| 页面截图复核 | `page-19.png`、`page-24.png` | 已人工确认：Fig. 10 新增 `(e)` 行，Fig. 14 legend 无编号且第三行从 `(f)` 开始 |
| layout-check | job `20260427_171612_71c9e340` | 报告 `issue_count=1`：第 23 页存在既有 underfilled page；与本次两处图版任务无直接对应关系 |

## 修改文件

- `src/visualization/subfigure_montage.py`
- `docs/paper/src/paper_pipeline.py`
- `docs/paper/latex/main.tex`
- `docs/paper/figures/fig_08_frequency_response_comparison.png`
- `docs/paper/figures/fig_08_frequency_response_comparison.raw.json`
- `docs/paper/figures/fig_18_hparam_sensitivity.png`
- `docs/paper/figures/fig_18_hparam_sensitivity.raw.json`
- `docs/paper/latex/build/main.pdf`
- `docs/paper/latex/build/page_screenshots_r2410/page-19.png`
- `docs/paper/latex/build/page_screenshots_r2410/page-24.png`
- 以及 paper pipeline 同步刷新的 `docs/paper/data/results.json`、`docs/paper/latex/values.tex` 等关联生成产物

## 备注

- 为满足“第三行独占两列”的版式要求，本次在公共拼图工具中新增了跨列能力；该能力不会改变既有单列/双列拼图的默认行为。
- 最终 PDF 中，Fig. 10 位于第 19 页，Fig. 13（源码文件 `fig_18_hparam_sensitivity.png`）位于第 24 页，二者的目标版式均已到位。

## 2026-04-27 晚间追加修订

### Fig. 10 再次重排

- 根据后续版式要求，`fig_08_frequency_response_comparison.png` 不再使用“第三行整行时域图”的旧方案，现改为：
  - 左上 `(d)`：频响 before/after 对比；
  - 右上 `(a)`、`(b)`：两张轨迹图竖排；
  - 左下 `(e)`：时域输出图；
  - 右下 `(c)`：单图四柱的 drift 对比图。
- 本次同时修正了 `make_bitmap_montage()` 未把 `row_span/col_span` 透传给 `PanelSpec` 的问题；此前 `Fig. 10` raw 元数据中 `(d)` 实际没有按跨行生效，导致子图落位与目标拓扑不一致。
- 重新平衡了 Fig. 10 各 panel 的 `fit_width/fit_height` 与列宽，使 `(d)` 不再过度压制 `(a)/(b)/(c)/(e)`。

### Fig. 11 一次拼 4 图确认

- `fig_05_onboard_inference.png` 已维持为一次性 `2 x 2` 拼图，不再做嵌套二次拼接。
- `Fig. 11(d)` 的 legend 已放到图外侧，避免与曲线和数据点重叠。
- 当前 raw 元数据确认 `Fig. 11` 为 `rows=2, cols=2`，四个 panel 共用同一套最终字号与缩放规则。

### 本轮验证

| 验证项 | 命令 / Job | 结果 |
| --- | --- | --- |
| 定向重生成 Fig. 10 / Fig. 11 | 内联 `python` 调用 `paper_pipeline.make_onboard_figure()` 与 `create_additional_paper_figures()` | 通过；刷新 `fig_05_onboard_inference.*` 与 `fig_08_frequency_response_comparison.*` |
| Fig. 10 raw 结构核对 | `fig_08_frequency_response_comparison.raw.json` | 通过；`(d)`=`row0,col0,row_span2`，`(a)`=`row0,col1`，`(b)`=`row1,col1`，`(e)`=`row2,col0`，`(c)`=`row2,col1` |
| Fig. 11 raw 结构核对 | `fig_05_onboard_inference.raw.json` | 通过；`rows=2, cols=2`，确认为单次 4 图拼接 |
| visual audit | 两个 raw 的 `visual_quality_audit.status` | `pass` |
| LaTeX probe | `latex-cli.py debug probe --workdir docs/paper/latex --tex main.tex` | 通过 |
| LaTeX build | job `20260427_200411_00561a4d` | 生成了 `build/main.pdf`，但 job 以 `returncode=1` 结束；日志显示为既有中文字符缺少字体 glyph 警告触发，非本轮拼图逻辑引入 |

### 说明

- 本轮没有重新跑完整 `python docs/paper/src/paper_pipeline.py`，因为该全量流程仍会受无关缺失文件 `projects/08_IIR_AB/FRIKANh8u6l6_e1k_lr2e4_sysiir_trainable/data/metrics.json` 阻塞；因此采用了只重生成 Fig. 10 / Fig. 11 的定向脚本。

## 2026-04-27 夜间细调

- `Fig. 10` 子图编号进一步调整为：频响对比 `(a)`、固有频率轨迹 `(b)`、灵敏度轨迹 `(c)`、时域输出 `(d)`、漂移柱状图 `(e)`。
- `Fig. 10` 中固有频率轨迹与灵敏度轨迹两张折线图的高度在上一版基础上再缩减约 `1/3`，以减轻右侧竖排区域的纵向压迫感。
- `Fig. 11(d)` 的 legend 从侧边改到顶部单行，并取消边框；同时保留图外放置，避免与曲线和双 y 轴数据重叠。
- `docs/paper/latex/main.tex` 已同步更新 Fig. 10 caption 与正文中的 `(a)--(e)` 对应关系。
- `paper-spec` 扫描 job `20260427_213517_1193ba6a` 结果为 `passed=true`，未发现新增正文风险项。

## 2026-04-27 深夜定稿核对

- 为进一步减小 Fig. 10 右侧竖排区域的中间留白，将 `(b)` 子图在其单元格内改为底对齐，使 `(b)` 与 `(c)` 的视觉间距明显收紧，同时保持原有尺寸与编号不变。
- 已重新定向生成 `fig_08_frequency_response_comparison.png` 与 `fig_05_onboard_inference.png`，两张图的 raw `visual_quality_audit` 仍为 `pass`。
- LaTeX 构建 job `20260427_214938_2f338a10` 已完成，`returncode=0`；`layout_check.status=passed`，`issue_count=0`。
- 本轮生成的页面截图位于 `docs/paper/latex/build/page_screenshots/`，其中：
  - `page-19.png`：Fig. 10 最新版；
  - `page-20.png`：Fig. 11 最新版。
