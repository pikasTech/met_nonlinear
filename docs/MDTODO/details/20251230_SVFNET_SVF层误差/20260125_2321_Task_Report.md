# R1.13 实现报告：SVF误差仿真结果与实测数据对比图

## 任务概述

**任务ID**: R1.13
**任务描述**: 增加一个对比图，对比 `svf_dense_error_comparison.png` 和 `frequency_response_comparison_merged.png`，新图中：
- 虚线: `SVF层（带误差）+ Dense 层`
- 实线: `实测的SVF层+Dense层`

**完成状态**: ✅ 已完成

## 实现方案

### 1. 配置修改

在 `config.json` 的 `svf_error_simulation` 配置节中新增 `r1_13_comparison` 配置项：

```json
"r1_13_comparison": {
  "enable": true,
  "output_filename": "svf_dense_vs_experiment_comparison.png"
}
```

### 2. 代码修改

#### 2.1 新增方法

1. **`_load_experiment_data_for_r1_13`** (`wnet5_circuit_validator.py:1363-1445`)
   - 从实验数据Excel文件加载实测的SVF+Dense层数据
   - 支持新的GAIN/CH格式和旧的D{layer}_N_GAIN/B1格式
   - 返回实验频率和增益数据

2. **`_generate_r1_13_comparison_plot`** (`wnet5_circuit_validator.py:1447-1539`)
   - 生成R1.13对比图
   - 虚线 = SVF仿真（带误差）+ Dense层
   - 实线 = 实测SVF+Dense层
   - 将实验数据插值到仿真频率点进行对比

#### 2.2 修改 `execute_validation` 方法

在SVF+Dense误差仿真完成后，调用R1.13对比图生成逻辑：

```python
# R1.13新增：SVF误差仿真结果与实测数据对比
r1_13_config = self.svf_error_config.get('r1_13_comparison', {})
r1_13_enable = r1_13_config.get('enable', False)
if r1_13_enable and include_dense:
    # 加载实验数据并生成对比图
```

#### 2.3 配置验证模式更新

在 `core/config_validator.py` 的 `WNET5_CIRCUIT_VALIDATION_SCHEMA` 中添加 `r1_13_comparison` 字段验证：

```python
"r1_13_comparison": {
    "type": "object",
    "additionalProperties": False,
    "properties": {
        "enable": {"type": "boolean"},
        "output_filename": {"type": "string", "minLength": 1}
    }
}
```

### 3. 配置文件更新

为以下项目启用R1.13对比功能：
- `WNET5q1h2u6l3_layer1/config.json`
- `WNET5q1h2u6l3_layer2/config.json`
- `WNET5q1h2u6l3_layer3/config.json`
- `WNET5q1h2u6l3_layer4/config.json`

## 验证结果

### 运行命令

```bash
conda.bat run --no-capture-output -n tf26 python cli.py ep ex_projects/inference/wnet5-circuit-validation/WNET5q1h2u6l3_layer1
```

### 输出图片

**图片路径**: `ex_projects/inference/wnet5-circuit-validation/WNET5q1h2u6l3_layer1/data/plots/svf_dense_vs_experiment_comparison.png`

**图片内容**:
- **标题**: `WNET5q1h2u6l3 Dense#1 SVF误差仿真 vs 实测对比 (虚线=SVF仿真(带误差)+Dense, 实线=实测SVF+Dense)`
- **横轴**: 频率 (Hz)，对数刻度
- **纵轴**: 增益 (线性, log刻度)
- **曲线**:
  - 虚线: `D1_1 (仿真误差)`、`D1_2 (仿真误差)`、`D1_3 (仿真误差)`、`D1_4 (仿真误差)`、`D1_5 (仿真误差)`、`D1_6 (仿真误差)`
  - 实线: `D1_1 (实测)`、`D1_2 (实测)`、`D1_3 (实测)`、`D1_4 (实测)`、`D1_5 (实测)`、`D1_6 (实测)`

### 图片风格

- 与 `svf_dense_error_comparison.png` 和 `frequency_response_comparison_merged.png` 保持一致的视觉风格
- 使用tab10颜色映射，6组颜色对应6个Dense输出通道
- 每个通道用相同颜色的实线和虚线表示，便于对比

## 修改文件清单

1. `visualization/wnet5_circuit_validator.py`
   - 新增 `_load_experiment_data_for_r1_13` 方法
   - 新增 `_generate_r1_13_comparison_plot` 方法
   - 修改 `execute_validation` 方法添加R1.13调用逻辑

2. `core/config_validator.py`
   - 在 `WNET5_CIRCUIT_VALIDATION_SCHEMA` 中添加 `r1_13_comparison` 字段验证

3. `ex_projects/inference/wnet5-circuit-validation/WNET5q1h2u6l3_layer1/config.json`
4. `ex_projects/inference/wnet5-circuit-validation/WNET5q1h2u6l3_layer2/config.json`
5. `ex_projects/inference/wnet5-circuit-validation/WNET5q1h2u6l3_layer3/config.json`
6. `ex_projects/inference/wnet5-circuit-validation/WNET5q1h2u6l3_layer4/config.json`

## 结论

R1.13任务已完成，成功实现了SVF误差仿真结果与实测数据的对比图生成功能。该功能：
- 虚线表示使用拟合参数计算的SVF（带误差）与Dense层组合的频率响应
- 实线表示实测的SVF+Dense层频率响应
- 可直观比较仿真误差与实际测试结果的差异
- 已为所有4个Dense层配置启用
