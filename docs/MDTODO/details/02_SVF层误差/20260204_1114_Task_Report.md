# R1.15.1 任务报告：frequency_response.png 添加补偿前假频曲线

## 任务目标

在 `ex_projects/inference/wnet5-circuit-validation/WNET5_EFF2_A1_PS-5_360/layer1/data/plots/frequency_response.png` 中额外绘制一条粗虚线，显示**补偿前的假频**频率响应。

## 实现步骤

### 1. 添加配置项

修改 `WNET5_EFF2_A1_PS-5_360/layer1/config.json`：

```json
{
  "experiment_comparison": {
    "enable": true,
    "mode": "multi_file",
    "experiment_data_dir": "exam_data/SVF-W_DENSE",
    "selftest_file": "exam_data/SVF-W_DENSE/output_20251103_085135_sweep_selftest_震级1.0.xlsx",
    "plot_config": {
      "show_pre_compensation_alias": true,
      "alias_label": "补偿前假频",
      "alias_line_style": "--",
      "alias_linewidth": 2.5
    }
  }
}
```

### 2. 更新配置验证 Schema

修改 `src/core/config_validator.py`，添加新字段验证：
- `show_pre_compensation_alias`: 是否显示补偿前假频曲线
- `alias_label`: 曲线标签
- `alias_line_style`: 线型
- `alias_linewidth`: 线宽

### 3. 修改绘图代码

修改 `src/visualization/wnet5_circuit_validator.py` 的 `_generate_plots_multi_file` 方法：

**保存原始实验数据**：
```python
# 保存补偿前假频数据
raw_exp_data_dict[channel] = {
    'frequencies': raw_data['frequencies'],
    'magnitude_db': raw_db
}
```

**绘制曲线**（风格与 `frequency_response_comparison.png` 一致）：
- 理论仿真：虚线 (`linestyle='--'`)
- 补偿后实测：实线 (`linestyle='-'`, 蓝色)
- 补偿前假频：粗虚线 (`linestyle='--'`, 灰色, linewidth=2.5)

## 生成结果

### 文件位置
```
ex_projects/inference/wnet5-circuit-validation/WNET5_EFF2_A1_PS-5_360/layer1/data/plots/
├── frequency_response.png                      # 含补偿前后对比曲线（本次新增）
├── frequency_response_comparison_multi.png      # 上下布局对比图
└── svf_frequency_response.png                  # SVF层频率响应
```

### 图表说明
图片风格与 `frequency_response_comparison.png` 一致：
| 曲线类型 | 线型 | 颜色 | 说明 |
|---------|------|------|------|
| 理论仿真 | 虚线 | 彩色 | 14个通道的理论频率响应 |
| 补偿后实测 | 实线 | 蓝色 | 经过自测试数据补偿的实验数据 |
| 补偿前假频 | 粗虚线 | 灰色 | 未经补偿的原始实验数据 |

### 运行命令

```bash
conda.bat run --no-capture-output -n tf26 python cli.py ep ex_projects/inference/wnet5-circuit-validation/WNET5_EFF2_A1_PS-5_360/layer1
```

## 验证结果

测试运行成功，输出了以下关键日志：
```
[INFO  15.70s] 📊 对比图已保存: frequency_response_comparison_multi.png
[INFO  15.70s] 📊 生成包含补偿前假频曲线的 frequency_response.png (R1.15)...
[INFO  17.07s] 📊 含补偿前假频的频率响应图已保存: frequency_response.png
```

## 结论

R1.15.1 任务成功完成：
- ✅ 在 `frequency_response.png` 中添加了补偿前假频曲线（粗虚线）
- ✅ 风格与 `frequency_response_comparison.png` 保持一致
- ✅ 复用现有基础设施，改动最小化
- ✅ 支持通过 `plot_config` 自定义曲线样式
- ✅ 显示三组对比：理论仿真、补偿后实测、补偿前假频
