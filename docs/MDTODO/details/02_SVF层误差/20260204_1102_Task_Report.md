# R1.15 调查报告：frequency_response.png 添加补偿前假频曲线

## 任务目标

在 `ex_projects/inference/wnet5-circuit-validation/WNET5_EFF2_A1_PS-5_360/layer1/data/plots/frequency_response.png` 中额外绘制一条曲线（粗虚线），显示**补偿前的假频**频率响应。

## 调查发现

### 1. 现有绘图代码位置

**主绘图函数**: `src/visualization/wnet5_circuit_validator.py`

| 行号 | 方法名 | 说明 |
|------|--------|------|
| 2534-2554 | 无实验数据时的理论图 | 仅绘制理论频率响应 |
| 2400-2436 | 有实验数据时的对比图 | 绘制理论与实验对比 |
| 2558-2745 | `_generate_plots_multi_file` | 多文件对比模式 |

### 2. 现有数据结构

**frequency_response 数据结构** (`results.json`):
```json
{
  "frequency_response": {
    "frequencies": [2.0, 2.5, ..., 500.0],  // 1000个频点
    "magnitude_db": [  // 14个通道
      {"index": 0, "magnitude_db": [10.22, ...]},  // 通道1
      ...
    ]
  }
}
```

**实验数据加载方法**:
| 方法 | 功能 |
|------|------|
| `_load_experiment_channel_data()` | 加载原始实验数据（xlsx格式） |
| `_load_selftest_data()` | 加载自测试数据 |
| `_compensate_with_selftest()` | 执行补偿: `exp_compensated = exp_mag / selftest_mag` |

### 3. 现有配置结构

当前 `WNET5_EFF2_A1_PS-5_360/layer1/config.json`:
```json
{
  "task_type": "wnet5-circuit-validation",
  "model_project_name": "WNET5_EFF2_A1_PS-5_360",
  "analysis_layer": 1,
  "frequency_range": {"start_freq": 2, "stop_freq": 500}
}
```

### 4. 实验对比配置项（需要添加）

```json
{
  "experiment_comparison": {
    "enable": true,
    "mode": "multi_file",
    "experiment_data_dir": "${MET_DATA_BASE}/data/experiment",
    "selftest_file": "${MET_DATA_BASE}/data/selftest.xlsx",
    "plot_config": {
      "show_pre_compensation_alias": true,
      "alias_line_style": "--",
      "alias_linewidth": 2.5
    }
  }
}
```

## 实现方案

### 方案1：复用现有 `_generate_plots_multi_file` 方法

**优点**: 改动最小，利用现有基础设施
**缺点**: 需要配置完整的实验数据

**修改位置**:
1. 在 `config.json` 中添加 `experiment_comparison` 配置
2. 在 `_generate_plots_multi_file` 方法中添加绘制"补偿前假频"的逻辑
3. 保存原始实验数据用于绘图

**核心代码逻辑**:
```python
# 在 _generate_plots_multi_file 中添加
if raw_exp_data is not None:
    # 绘制补偿前假频（粗虚线）
    raw_db = 20 * np.log10(raw_exp_data['magnitude'] + 1e-20)
    ax.semilogx(raw_exp_data['frequencies'], raw_db,
                color='gray', linestyle='--', linewidth=2.5,
                label='补偿前假频')
```

### 方案2：修改 `frequency_response.png` 生成逻辑

**优点**: 直接在主图中添加
**缺点**: 需要较大的代码修改

**修改位置**:
1. 在 `_generate_plots` 方法中添加新配置项
2. 加载原始实验数据
3. 添加额外的曲线绘制代码

## 推荐方案

**推荐方案1**，理由：
1. 改动最小
2. 复用现有 `_generate_plots_multi_file` 方法
3. 架构清晰，易于维护

## 实施步骤

### 步骤1：添加配置项到 config.json

```json
{
  "experiment_comparison": {
    "enable": true,
    "mode": "multi_file",
    "experiment_data_dir": "path/to/experiment_data",
    "selftest_file": "path/to/selftest.xlsx",
    "plot_config": {
      "show_pre_compensation_alias": true,
      "alias_label": "补偿前假频",
      "alias_line_style": "--",
      "alias_linewidth": 2.5
    }
  }
}
```

### 步骤2：修改绘图代码

在 `wnet5_circuit_validator.py` 的 `_generate_plots_multi_file` 方法中：
1. 保存原始实验数据（未补偿）
2. 在图表中添加粗虚线曲线

### 步骤3：验证

运行 `python cli.py ep ex_projects/inference/wnet5-circuit-validation/WNET5_EFF2_A1_PS-5_360/layer1` 验证结果。

## 关键代码位置索引

| 功能 | 文件 | 行号 |
|------|------|------|
| 加载实验数据 | wnet5_circuit_validator.py | 284-336 |
| 加载自测试数据 | wnet5_circuit_validator.py | 99-150 |
| 补偿计算 | wnet5_circuit_validator.py | 338-391 |
| 绘图代码 | wnet5_circuit_validator.py | 2534-2554 |
| 多文件对比绘图 | wnet5_circuit_validator.py | 2558-2745 |

## 结论

R1.15 任务可以通过**复用现有基础设施**实现最小改动：
1. 在 `config.json` 中添加 `experiment_comparison` 配置
2. 修改 `_generate_plots_multi_file` 方法保存并绘制原始实验数据
3. 样式：粗虚线（linewidth=2.5, linestyle='--'）

目前是调查阶段，不实际修改代码。实施方案已明确，可以进入实现阶段。
