# R1.14 任务报告：SVF层频率响应图生成

## 任务目标

在 `wnet5-circuit-validation` 绘图时，多绘制一个 SVF 层的频率响应图，生成的图保存到 `ex_projects/inference/wnet5-circuit-validation/WNET5q1h2u6l3_layer1/data/plots/` 目录下。

## 实现方案

### 1. 添加新方法 `_generate_svf_frequency_response_plot`

在 `src/visualization/wnet5_circuit_validator.py` 中添加新方法，位置在 `_generate_svf_error_comparison_plot` 方法之后（约第621行）。

**方法功能**：
- 生成纯 SVF 层的理论频率响应图（不包含实测数据对比）
- 绘制所有 SVF 滤波器输出（LP, BP, HP）
- 风格与 `frequency_response_comparison_merged.png` 保持一致：
  - 单图显示
  - semilogx 坐标系
  - 线性增益

**代码位置**：`src/visualization/wnet5_circuit_validator.py:621-689`

### 2. 修改 `execute_validation()` 方法

在 `else` 分支（`svf_error_enable=False` 时）添加对新方法的调用：

```python
# R1.14新增：生成纯SVF层频率响应图
# 计算纯SVF层的频率响应（不含Dense层）
svf_response_only = self._calculate_svf_frequency_response_only(svf_params)
svf_freq_plot = self._generate_svf_frequency_response_plot(svf_response_only)
plots.append(svf_freq_plot)
```

**代码位置**：`src/visualization/wnet5_circuit_validator.py:1786-1790`

### 3. 输出文件

生成的图片：`svf_frequency_response.png`

保存位置：`ex_projects/inference/wnet5-circuit-validation/WNET5q1h2u6l3_layer1/data/plots/svf_frequency_response.png`

## 验证结果

运行命令：
```bash
conda.bat run --no-capture-output -n tf26 python cli.py ep ex_projects/inference/wnet5-circuit-validation/WNET5q1h2u6l3_layer1
```

日志输出：
```
[INFO  15.33s] ✅ SVF层理论频响计算完成: 6 个通道
[INFO  17.03s] SVF层频率响应图已保存: F:\Work\met_nonlinear_worktrees\met_nonlinear_master\ex_projects\inference\wnet5-circuit-validation\WNET5q1h2u6l3_layer1\data\plots\svf_frequency_response.png
```

生成的图片列表：
- `svf_frequency_response.png` - SVF层理论频率响应图（新增）
- `frequency_response_comparison_merged.png` - Dense层频率响应对比图
- `frequency_response_error_ratio.png` - 误差比值图

## 总结

成功实现了 R1.14 任务需求：

1. ✅ 复用了现有的 `_calculate_svf_frequency_response_only` 方法计算 SVF 层频率响应
2. ✅ 添加了新的 `_generate_svf_frequency_response_plot` 方法生成频率响应图
3. ✅ 生成的图保存到指定目录 `data/plots/`
4. ✅ 风格与其他图表保持一致

该功能在 `svf_error_simulation.enable=false` 时会自动启用，生成 SVF 层的纯理论频率响应图。
