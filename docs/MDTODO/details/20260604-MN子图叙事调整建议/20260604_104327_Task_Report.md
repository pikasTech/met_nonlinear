# R1.1 任务报告

## 任务

按照 R1 的叙事建议完成 MN 主文子图调整，将 Supplement 中推荐回主文的子图并回主图，并确保调整后的子图仍由 WebUI/Figure Studio 可操作的拼图工程管理。

## 调整范围

本次只执行 R1 中收益最高的两处调整：

1. 将 MET 大信号非线性机理图并回第一张主文器件图。
2. 将 local transfer slices 并回 Wiener-KAN 方法图。

未将 hyperparameter sensitivity、LUT principle、KAN single-neuron toy example、parallel Wiener 结果型 panel 或部署 workflow 等内容继续塞回主文，避免违背 R1 中“一张图只回答同一层级问题”的原则。

## 具体修改

### Fig. 1 对象与问题来源图

修改 `ex_projects/plot/multi/fig_20_met_structure_readout_montage/config.json`：

- 原两面板 `MET structure + readout circuit` 扩展为三面板矩阵拼图。
- 新增 `ex_projects/plot/single/fig_14_met_nonlinear_mechanism` 作为 `(c)` 面板。
- 保持所有面板通过 `paper_figure.subfigures[].project_path` 引用 leaf ex_project，未直接拼接最终位图。
- 为新增机理子图补充 `parent_montages`，便于 Figure Studio 中追踪父级拼图关系。

同步修改主稿：

- `docs/paper/latex/main.translated.tex`
- `docs/paper/latex/main.tex`

Fig. 1 caption 已更新为 `(a)` MET 结构、`(b)` 读出电路、`(c)` 大信号非线性机理；正文中幅频耦合机理引用转为 `Fig.~\ref{fig:met_structure_readout}(c)`。

### Fig. 4 方法图

新增拼图工程：

- `ex_projects/plot/multi/fig_23_wiener_kan_framework_local_transfer_montage/config.json`

该工程以 `local_transfer_slices` 同类 montage 配置为模板建立，包含三个 leaf 子图：

- `(a)` `ex_projects/plot/single/wiener_kan_framework`
- `(b)` `ex_projects/plot/single/response_original_temp`
- `(c)` `ex_projects/plot/single/response_compensated_temp`

布局采用 `matrix`，左侧 framework 跨两行，右侧上下排列两个 local transfer slices。`figure_config.panels` 和 `figure_config.montage` 中保留尺寸、对齐、trim、gutter、label 等可调字段，供 WebUI/Figure Studio 继续调整。

同步修改主稿：

- `docs/paper/latex/main.translated.tex`
- `docs/paper/latex/main.tex`

方法图引用从原单图 `wiener_kan_framework.png` 改为新拼图 `fig_23_wiener_kan_framework_local_transfer_montage.png`；caption 已明确 `(a)` 框架、`(b)` 局部实测频率响应、`(c)` 相对传递函数初始化。旧标签 `fig:FRIRNN_3D_response_slices` 保留在新方法图上，以避免历史交叉引用断裂。

## WebUI/Figure Studio 可操作性

本次调整没有使用手工 PNG 粘贴方式。两个主图均由 `ex_projects/plot/multi/**/config.json` 驱动：

- `fig_20_met_structure_readout_montage`
- `fig_23_wiener_kan_framework_local_transfer_montage`

执行 `python src/visualization/paper_figure_projects.py list` 后可以扫描到上述两个 montage 工程，以及相关 leaf 子图工程。两张图均可通过 `python cli.py ep <project>` 重新生成，满足 Figure Studio 读取配置、调整参数、重绘拼图的入口要求。

## 验证

已执行：

- `python cli.py ep ex_projects/plot/multi/fig_20_met_structure_readout_montage`
- `python cli.py ep ex_projects/plot/multi/fig_23_wiener_kan_framework_local_transfer_montage`
- `python src/visualization/paper_figure_projects.py list`
- `python C:\Users\lyon\.agents\skills\latex\scripts\latex-cli.py debug probe --workdir C:\work\met_nonlinear_master\docs\paper\latex --tex main.translated.tex`
- `python C:\Users\lyon\.agents\skills\latex\scripts\latex-cli.py build --workdir C:\work\met_nonlinear_master\docs\paper\latex --tex main.translated.tex --engine xelatex --passes 2`

结果：

- 两张新拼图均成功生成 PNG 与 `.raw.json`。
- 已目视检查 `fig_20_met_structure_readout_montage.png` 和 `fig_23_wiener_kan_framework_local_transfer_montage.png`，子图完整、标签可读、未发现明显遮挡。
- LaTeX 构建 job `20260604_110923_5780c8ad` 成功，`returncode = 0`。
- 输出 PDF：`docs/paper/latex/build/main.translated.pdf`。
- layout check 通过，`issue_count = 0`。
- 主文浮动统计为 6 个 figure、2 个 table，总 display item 仍为 8 个；本次调整通过合并拼图完成，没有增加主文展示项。

## 备注

LaTeX build 日志中由构建器启动的 paper-spec 扫描子任务状态文件保持 `unknown`，但对应日志显示扫描已结束并给出 17 条 warning。该扫描结果未导致本次 LaTeX 构建失败；本任务范围为 R1.1 子图拼图与主文引用调整，未扩展处理全文写作风格 warning。
