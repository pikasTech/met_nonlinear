# R2.4.1 任务报告

## 任务概述
执行修改 model_engine.py 添加 CNNKAN 分支任务

## 任务内容
在 `src/core/model_engine.py` 的 `build_model` 方法中添加 CNNKAN 模型的分支支持。

## 修改文件
- `src/core/model_engine.py`

## 修改详情

### 修改位置
`src/core/model_engine.py` 第 398-418 行

### 添加的代码
在 RVTDCNN 分支后、else 分支前添加了 CNNKAN 分支：

```python
elif self.config.use_model == 'CNNKAN':
    self.model_comp = CNNKAN(
        fs=self.config.sample_rate,
        grid_size=self.config.GRID_SIZE,
        grid_range=(
            self.config.grid_range[0], self.config.grid_range[1]),
        spline_order=self.config.SPLINE_ORDER,
        basis_activation=self.config.basis_activation,
        fix_scale_factor=self.config.FIX_SCALE_FACTOR,
        inner_kan_units=self.config.INNER_KAN_UNITS,
        inner_kan_layers=self.config.INNER_KAN_LAYERS,
        use_fast_model=self.config.USE_FAST_MODEL,
        checkpoint_dir=self.checkpoint_dir,
        kan_log_grid=self.config.kan_log_grid,
        kan_grid_expand=self.config.kan_grid_expand,
        save_each_epoch=self.config.save_each_epoch,
        model_subcfg=self.config.model_subcfg,
        cnn_filters=self.config.CNN_FILTERS,
        cnn_kernel_size=self.config.CNN_KERNEL_SIZE,
        dropout_rate=self.config.CNN_DROPOUT_RATE,
    )
```

## 参数映射说明

| CNNKAN 参数 | 配置来源 | 说明 |
|------------|---------|------|
| fs | self.config.sample_rate | 采样频率 |
| grid_size | self.config.GRID_SIZE | KAN 网格大小 |
| grid_range | self.config.grid_range | 网格范围 |
| spline_order | self.config.SPLINE_ORDER | 样条阶数 |
| basis_activation | self.config.basis_activation | 基底激活函数 |
| fix_scale_factor | self.config.FIX_SCALE_FACTOR | 是否固定缩放因子 |
| inner_kan_units | self.config.INNER_KAN_UNITS | 内部 KAN 单元数 |
| inner_kan_layers | self.config.INNER_KAN_LAYERS | 内部 KAN 层数 |
| use_fast_model | self.config.USE_FAST_MODEL | 是否使用快速模型 |
| checkpoint_dir | self.checkpoint_dir | 检查点目录 |
| kan_log_grid | self.config.kan_log_grid | 是否使用对数网格 |
| kan_grid_expand | self.config.kan_grid_expand | 是否扩展网格 |
| save_each_epoch | self.config.save_each_epoch | 是否每轮保存 |
| model_subcfg | self.config.model_subcfg | 模型子配置 |
| cnn_filters | self.config.CNN_FILTERS | Conv1D 输出通道数 (h8=8) |
| cnn_kernel_size | self.config.CNN_KERNEL_SIZE | Conv1D 卷积核大小 |
| dropout_rate | self.config.CNN_DROPOUT_RATE | Dropout 比率 |

## 设计说明

CNNKAN 模型不需要传入 `system` 参数（参考 LSTM 的写法），因此采用与其他非 FRIKAN 模型（如 LSTM、GRN、RVTDCNN）相同的直接实例化方式，而非 FRIKAN 使用的 `eval()` + `fromSystem()` 模式。

## 后续任务
- R2.4.2: 审查 R2.4.1 的执行结果
- R2.5: 创建 config.json 配置文件（需确保配置文件中包含 CNN_FILTERS, CNN_KERNEL_SIZE, CNN_DROPOUT_RATE 等参数）
- R2.6: 验证 CLI 训练功能
- R2.7: 验证 CLI 评估功能
