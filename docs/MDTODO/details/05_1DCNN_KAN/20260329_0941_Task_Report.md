# R1.1 任务报告：1DCNN_KAN 实现详细计划（方案2）

**任务**: 基于方案2（完整重写）做详细实现计划  
**参考模型**: FRIKANh8u6l6  
**日期**: 2026-03-29

---

## 1. 方案2概述

### 1.1 核心思路
直接继承 `BaseModel` 构建全新的 `CNNKAN` 模型，用 `Conv1D` 完全替换 `IIR` 滤波器层，保持 `KAN` 网络结构不变。

### 1.2 模型架构
```
Input(1) → Conv1D(filters=8, kernel_size=3) → DenseKAN×6 → DenseKAN → Output(1)
              ↓
         h8=8通道输出（1DCNN输出通道数）
```

### 1.3 与FRIKAN的关键差异

| 组件 | FRIKAN | 1DCNN_KAN |
|------|--------|-----------|
| 滤波器层 | IIR (SIMOIIR/DIAGIIR) | Conv1D |
| 滤波器特性 | 递归，依赖历史输出 | 非递归，依赖输入卷积 |
| 参数来源 | 从系统辨识获取 | 可学习卷积核 |
| 序列依赖 | 长期依赖 | 有限窗口(k=3) |

---

## 2. 文件修改清单

### 2.1 需要创建的文件

| 序号 | 文件路径 | 说明 |
|------|----------|------|
| 1 | `projects/1DCNN_KANh8u6l6/config.json` | 项目配置 |
| 2 | `projects/1DCNN_KANh8u6l6/data/` | 数据目录（空目录） |

### 2.2 需要修改的文件

| 序号 | 文件路径 | 修改内容 |
|------|----------|----------|
| 1 | `src/models/frikan_models.py` | 添加 `CNNKAN` 类 |

---

## 3. 详细代码修改点

### 3.1 `src/models/frikan_models.py` 添加新类

在文件末尾（`return x` 之后）添加以下代码：

```python
class CNNKAN(BaseModel):
    """
    CNNKAN (1D CNN based FRIKAN) 模型
    
    用 Conv1D 完全替换 IIR 滤波器层，直接继承 BaseModel 构建新模型。
    """

    def __init__(self,
                 iir_params_list=None,
                 grid_size=5,
                 grid_range=(-1.0, 1.0),
                 spline_order=3,
                 basis_activation='silu',
                 fs=2000,
                 checkpoint_dir='data',
                 fix_scale_factor=True,
                 disable_basis_activation=True,
                 inner_kan_units=4,
                 inner_kan_layers=1,
                 use_fast_model=True,
                 cnn_filters=8,
                 cnn_kernel_size=3,
                 dropout_rate=0.2,
                 kan_log_grid=False,
                 kan_grid_expand=True,
                 save_each_epoch=False,
                 model_subcfg={}
                 ):
        """
        初始化CNNKAN模型

        Args:
            iir_params_list: 保留参数接口兼容性（实际不使用）
            grid_size: KAN网络的网格大小
            grid_range: 网格范围，如(-1.0, 1.0)
            spline_order: 样条阶数
            basis_activation: 基底激活函数
            fs: 采样频率
            checkpoint_dir: 检查点目录
            fix_scale_factor: 是否固定缩放因子
            disable_basis_activation: 是否禁用基底激活
            inner_kan_units: 内部KAN单元数量
            inner_kan_layers: 内部KAN层数
            use_fast_model: 是否使用快速模型
            cnn_filters: Conv1D 输出通道数 (h8 = 8)
            cnn_kernel_size: Conv1D 卷积核大小
            dropout_rate: Dropout比率
            kan_log_grid: 是否使用对数网格
            kan_grid_expand: 是否扩展网格
            save_each_epoch: 是否每个周期保存
            model_subcfg: 模型子配置
        """
        self.model_name = 'CNNKAN'
        self.callback = None
        
        # Conv1D 层替代 IIR
        self.cnn_filters = cnn_filters
        self.cnn_kernel_size = cnn_kernel_size
        self.cnn = tf.keras.layers.Conv1D(
            filters=cnn_filters,
            kernel_size=cnn_kernel_size,
            strides=1,
            padding='same',
            activation='linear',
            name='cnn_filter'
        )
        
        # 快速模型也使用 Conv1D
        self.fast_cnn = tf.keras.layers.Conv1D(
            filters=cnn_filters,
            kernel_size=cnn_kernel_size,
            strides=1,
            padding='same',
            activation='linear',
            name='fast_cnn_filter'
        )
        
        # 输出 KAN 层
        self.kan = DenseKAN(
            units=1,
            grid_size=grid_size,
            grid_range=grid_range,
            spline_order=spline_order,
            use_bias=True,
            basis_activation=basis_activation,
            fix_scale_factor=fix_scale_factor,
            disable_basis_activation=disable_basis_activation,
            kan_log_grid=kan_log_grid,
            grid_expand=kan_grid_expand
        )

        # 内层 KAN 层
        self.kan_inner_layers = [
            DenseKAN(
                units=inner_kan_units,
                grid_size=grid_size,
                grid_range=grid_range,
                spline_order=spline_order,
                use_bias=True,
                basis_activation=basis_activation,
                fix_scale_factor=fix_scale_factor,
                disable_basis_activation=disable_basis_activation,
                kan_log_grid=kan_log_grid,
                grid_expand=kan_grid_expand
            ) for _ in range(inner_kan_layers)
        ]

        self.dropout_rate = dropout_rate
        self.dropout_layer = tf.keras.layers.Dropout(
            self.dropout_rate) if self.dropout_rate > 0.0 else None
        self.fs = fs
        self.features_num = cnn_filters
        self.use_fast_model = use_fast_model
        self.dropout_position = 'input'
        self.save_each_epoch = save_each_epoch
        self.init_checkpoint(checkpoint_dir)
        self.build_model()

    def build_model(self):
        """
        构建并编译模型
        """
        # 输入层
        input_layer = tf.keras.layers.Input(shape=(None, 1), name='input')
        if self.dropout_layer is not None and self.dropout_position == 'input':
            input_drop_out = self.dropout_layer(input_layer)
        else:
            input_drop_out = input_layer

        # CNN 层
        cnn_out = self.cnn(input_drop_out)
        fast_input = tf.keras.layers.Input(
            shape=(None, self.cnn_filters), name='fast_input')
        fast_cnn_out = self.fast_cnn(fast_input)

        # dropout
        if self.dropout_layer is not None and self.dropout_position == 'cnn':
            cnn_drop_out = self.dropout_layer(cnn_out)
            fast_cnn_drop_out = self.dropout_layer(fast_cnn_out)
        else:
            cnn_drop_out = cnn_out
            fast_cnn_drop_out = fast_cnn_out

        # KAN 内层
        kan_inner_output = self.build_kan_inner_layers(cnn_drop_out)
        fast_kan_inner_output = self.build_kan_inner_layers(fast_cnn_drop_out)

        # dropout
        if self.dropout_layer is not None and self.dropout_position == 'output':
            kan_inner_output = self.dropout_layer(kan_inner_output)
            fast_kan_inner_output = self.dropout_layer(fast_kan_inner_output)

        # 输出层
        output = self.kan(kan_inner_output)
        fast_output = self.kan(fast_kan_inner_output)

        self.model = tf.keras.Model(
            inputs=input_layer, outputs=output, name='CNNKAN')
        self.model.build(input_shape=(None, None, 1))
        
        if self.use_fast_model:
            self.fast_model = tf.keras.Model(
                inputs=fast_input, outputs=fast_output, name='fast_CNNKAN')
            self.fast_model.build(input_shape=(None, None, self.cnn_filters]))

    @classmethod
    def fromSystem(cls,
                   hi_list,
                   fs,
                   grid_size=5,
                   grid_range=(-1.0, 1.0),
                   spline_order=3,
                   basis_activation='silu',
                   checkpoint_dir='data',
                   fix_scale_factor=True,
                   disable_basis_activation=True,
                   inner_kan_units=4,
                   inner_kan_layers=1,
                   use_fast_model=True,
                   use_debug=False,
                   cnn_filters=8,
                   cnn_kernel_size=3,
                   dropout_rate=0.2,
                   kan_log_grid=False,
                   kan_grid_expand=True,
                   save_each_epoch=False,
                   model_subcfg={}
                   ):
        """
        从系统参数构建CNNKAN模型

        Args:
            hi_list: 系统参数列表（保留接口兼容性，实际不使用）
            fs: 采样频率
            grid_size: 网格大小
            grid_range: 网格范围
            spline_order: 样条阶数
            basis_activation: 基底激活函数
            checkpoint_dir: 检查点目录
            fix_scale_factor: 是否固定缩放因子
            disable_basis_activation: 是否禁用基底激活
            inner_kan_units: 内层KAN单元数
            inner_kan_layers: 内层KAN层数
            use_fast_model: 是否使用快速模型
            use_debug: 是否启用调试
            cnn_filters: Conv1D 输出通道数
            cnn_kernel_size: Conv1D 卷积核大小
            dropout_rate: Dropout比率
            kan_log_grid: 是否使用对数网格
            kan_grid_expand: 是否扩展网格
            save_each_epoch: 是否每个周期保存
            model_subcfg: 模型子配置

        Returns:
            CNNKAN模型实例
        """
        model = cls(
            iir_params_list=None,  # 不使用 IIR 参数
            grid_size=grid_size,
            grid_range=grid_range,
            spline_order=spline_order,
            basis_activation=basis_activation,
            fs=fs,
            fix_scale_factor=fix_scale_factor,
            checkpoint_dir=checkpoint_dir,
            disable_basis_activation=disable_basis_activation,
            inner_kan_units=inner_kan_units,
            inner_kan_layers=inner_kan_layers,
            use_fast_model=use_fast_model,
            cnn_filters=cnn_filters,
            cnn_kernel_size=cnn_kernel_size,
            dropout_rate=dropout_rate,
            kan_log_grid=kan_log_grid,
            kan_grid_expand=kan_grid_expand,
            save_each_epoch=save_each_epoch,
            model_subcfg=model_subcfg
        )
        
        if use_debug:
            print(f"CNNKAN created with cnn_filters={cnn_filters}, cnn_kernel_size={cnn_kernel_size}")
        
        return model

    def build_kan_inner_layers(self, cnn_out):
        """
        构建KAN内层

        Args:
            cnn_out: CNN输出

        Returns:
            处理后的张量
        """
        x = cnn_out
        for i, kan_inner in enumerate(self.kan_inner_layers):
            x = kan_inner(x)
            if i + 1 == len(self.kan_inner_layers) // 2:
                if self.dropout_layer is not None and self.dropout_position == 'inner':
                    x = self.dropout_layer(x)
        return x
```

### 3.2 `projects/1DCNN_KANh8u6l6/config.json` 配置文件

```json
{
    "using_gpu": true,
    "epoch_train": 30000,
    "step_per_epoch": 5,
    "learning_rate": 0.01,
    "use_train_model": true,
    "use_model": "CNNKAN",
    "use_power_loss": true,
    "use_points": 8000,
    "resume_training": true,
    "use_best_val_weights": true,
    "SPLINE_ORDER": 2,
    "use_auto_lr": true,
    "auto_lr_decay_steps": 100,
    "IIR_TRAINABLE": false,
    "USE_FAST_MODEL": true,
    "IIR_INIT_BY_SYSTEM": true,
    "USE_ASSIGN_WEIGHTS": false,
    "use_scale": true,
    "use_predict_features": true,
    "use_predict_tr": false,
    "use_predict_tr_from_file": false,
    "use_predict_fr": true,
    "use_cache_features": true,
    "use_spline": false,
    "use_sin_fr": false,
    "use_debug_plot": false,
    "sample_rate": 2000,
    "time_clipped_s": 4.0,
    "GRID_SIZE": 8,
    "FIX_SCALE_FACTOR": true,
    "H_UNITS": 8,
    "INNER_KAN_UNITS": 6,
    "INNER_KAN_LAYERS": 6,
    "basis_activation": "tanh",
    "feature_range": [
        -1,
        1
    ],
    "target_sweep": 2,
    "AUG_TIMES": 1,
    "CNN_FILTERS": 8,
    "CNN_KERNEL_SIZE": 3
}
```

---

## 4. 实现步骤

### 步骤1: 创建项目目录结构
```bash
mkdir -p projects/1DCNN_KANh8u6l6/data
```

### 步骤2: 修改 `src/models/frikan_models.py`
- 在文件末尾添加 `CNNKAN` 类（约300行代码）

### 步骤3: 创建 `projects/1DCNN_KANh8u6l6/config.json`
- 复制自 `FRIKANh8u6l6` 配置
- 修改 `use_model` 为 `CNNKAN`
- 添加 `CNN_FILTERS` 和 `CNN_KERNEL_SIZE` 参数

### 步骤4: 验证 CLI 训练功能
```bash
python cli.py --project 1DCNN_KANh8u6l6 --mode train
```

### 步骤5: 验证 CLI 评估功能
```bash
python cli.py --project 1DCNN_KANh8u6l6 --mode eval
```

---

## 5. 技术细节

### 5.1 Conv1D 层参数
- `filters=8`: 输出通道数，对应 h8
- `kernel_size=3`: 卷积核大小，感受野为3个时间步
- `padding='same'`: 保持时间序列长度不变
- `activation='linear'`: 线性激活（后续 KAN 处理非线性）

### 5.2 与 model_engine.py 的兼容性
`model_engine.py` 中的 `build_model()` 方法通过以下逻辑处理 CNNKAN：
```python
if 'FRIKAN' in self.config.use_model:
    mod_class = eval(self.config.use_model)  # CNNKAN
    self.model_comp = mod_class.fromSystem(...)
```
由于 `CNNKAN.fromSystem()` 保留接口兼容性，可直接使用。

### 5.3 快速模型 (fast_model)
- 保留 `use_fast_model` 机制
- `fast_cnn` 与 `cnn` 结构相同，训练时直接处理输入序列

---

## 6. 配置参数对照表

| 配置项 | FRIKANh8u6l6 | 1DCNN_KANh8u6l6 | 说明 |
|--------|---------------|------------------|------|
| `use_model` | FRIKAN | CNNKAN | 模型类型 |
| `H_UNITS` | 8 | 8 | IIR通道数 → CNN通道数 |
| `INNER_KAN_UNITS` | 6 | 6 | 内层KAN单元数 |
| `INNER_KAN_LAYERS` | 6 | 6 | 内层KAN层数 |
| `GRID_SIZE` | 8 | 8 | 样条网格大小 |
| `SPLINE_ORDER` | 2 | 2 | 样条阶数 |
| `basis_activation` | tanh | tanh | 基底激活函数 |
| `CNN_FILTERS` | - | 8 | 新增：Conv1D通道数 |
| `CNN_KERNEL_SIZE` | - | 3 | 新增：卷积核大小 |

---

## 7. 关键代码修改点汇总

| 文件 | 修改类型 | 修改点 |
|------|----------|--------|
| `src/models/frikan_models.py` | 新增类 | 文件末尾添加 `CNNKAN` 类（~300行） |
| `projects/1DCNN_KANh8u6l6/config.json` | 新建文件 | 创建项目配置 |
| `projects/1DCNN_KANh8u6l6/data/` | 新建目录 | 空数据目录 |

---

## 8. 风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Conv1D 无法捕获长期依赖 | 中 | KAN 层可补偿部分非线性建模能力 |
| 训练不稳定 | 低 | 从小学习率开始，使用 `use_auto_lr` |
| 与现有 FRIKAN 变体接口不一致 | 低 | 保持 `fromSystem()` 接口完全兼容 |

---

## 9. 结论

R1.1 详细计划已完成。方案2的核心思想是：
1. 完全重写滤波器层，用 `Conv1D` 替换 `IIR`
2. 继承 `BaseModel` 构建新模型
3. 保持 `KAN` 层结构不变
4. 通过 `fromSystem()` 接口保持与现有训练流程兼容

实现工作量约为 300 行代码，创建一个新类和一个配置文件。
